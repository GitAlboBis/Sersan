{
  "subsystem": "R3F island — src/webgl/FounderPortraitMorph.tsx (892 lines), plus the two files it is coupled to: src/webgl/gpgpu/gpgpuNodeSim.ts (createTextMorphComputeBuild + PortraitMorphOpts) and src/webgl/image/sampleImagePoints.ts (samplePortraitPair)",
  "summary": "HOW THE TWO-TARGET ASSUMPTION IS ENCODED (verified by reading every line):\n\n(1) ASSET/LOADING. `FOUNDER_SLUGS = [\"alessandro\",\"michele\"]` (line 168) with `HEADSHOT_EXTS = [\"webp\",\"jpg\",\"png\"]` (167). `loadFounder(idx)` (219-229) tries `/founders/<slug>-headshot.<ext>` in order, falling back to `founders[idx]?.image ?? \"\"`. Note `loadImg(\"\")` REJECTS and the load effect's `.catch(() => {})` (316) swallows it — a missing third slug silently kills the ENTIRE cloud, not just target C. The load effect (297-322) does `Promise.all([loadFounder(0), loadFounder(1), import(...)])` then `mod.samplePortraitPair(a, b, sampleSpec())` (313) — hard-coded arity 2. Caches: `imgARef`/`imgBRef` (241-242), one `pairRef` (243).\n\n(2) SAMPLER. `samplePortraitPair` (sampleImagePoints.ts:398-435) builds the shared cell list as the UNION of exactly two inks: `if (Math.max(readA.ink[i], readB.ink[i]) > spec.inkCut) hits.push(i)` (412-414), strides down to `spec.maxCount`, returns `{ a: emit(readA,...), b: emit(readB,...), count, sharedCells, stride, gridW, gridH }`. `PortraitPair` (100-111) has exactly `a` and `b`.\n\n(3) COMPUTE WIRING. Lines 459-504: `createTextMorphComputeBuild(gl, webgpu, tsl, homeA, homeB, homeB, homeB, count, {...}, seed, { colorsA: sA.rgb, colorsB: sB.rgb, sizeA: sA.ink, sizeB: sB.ink, ... })` — homeC/homeD are homeB; 506-507 pin `uMorph2 = uMorph3 = 0` forever.\n\n(4) *** THE HIGH-RISK SEMANTICS — POSITION IS CHAINED, COLOUR/INK IS NOT ***\nPOSITION (gpgpuNodeSim.ts:1008-1020) is genuinely SEQUENTIAL/CHAINED — exactly what A→B→C needs:\n    const r = hash(instanceIndex).toVar();\n    const m = clamp(morphN.sub(r.mul(0.55)).div(0.45), 0.0, 1.0).toVar();\n    const target = mix(hA, hB, smoothstep(0.0, 1.0, m)).toVar();\n    const m2 = clamp(morph2N.sub(r.mul(0.55)).div(0.45), 0.0, 1.0).toVar();\n    target.assign(mix(target, hC, smoothstep(0.0, 1.0, m2)));\n    const m3 = clamp(morph3N.sub(r.mul(0.55)).div(0.45), 0.0, 1.0).toVar();\n    target.assign(mix(target, hD, smoothstep(0.0, 1.0, m3)));\n`uMorph2` blends FROM the already-blended target, NOT from homeA. With uMorph pinned at 1 while uMorph2 sweeps 0→1, every particle travels B→C. No wrong-path hazard on POSITION. The stagger saturates exactly: at uMorph = 1, m = (1 − 0.55·r)/0.45 ≥ 1 for all r∈[0,1), so target == hB EXACTLY before leg 2 opens. Sequential drive (finish leg 1, then leg 2) is therefore mandatory and correct; a parallel drive would be wrong.\n\nCOLOUR AND INK ARE THE TRAP. They know only A and B and are driven by `morphN` ALONE:\n    const portraitMorphExpr = hasPortrait ? smoothstep(0.0,1.0, clamp(morphN.sub(hash(instanceIndex).mul(0.55)).div(0.45),0.0,1.0)) : null;   // 1167-1173\n    const portraitInkExpr = hasPortraitSize ? mix(sizeABuffer!.toAttribute(), sizeBBuffer!.toAttribute(), portraitMorphExpr!) : null;         // 1175-1181\nand in the fragment (1371-1373): `const base = mix(cA, cB, vMorphColorF!).toVar();`\nCONSEQUENCE OF NAIVE WIRING: advancing only uMorph2 moves POSITIONS to Mattia's silhouette while every particle keeps MICHELE's pixel colour and MICHELE's ink. Ink drives disc size AND alpha (PORTRAIT_SIZE_MIN/INK 1188-1191; alpha knee 1418; sub-pixel coverage 1426-1431), so cells that are subject in Mattia but backdrop in Michele stay collapsed and are Discarded (1446) — Mattia renders as a Michele-shaped stencil in Michele's colours. THE SHADER MUST BE EXTENDED. This is the mandatory non-island change.\n\n(5) FIT / FRAMING (366-408). `halfX = Math.max(sA.halfExtentX, sB.halfExtentX, 1e-3)`, same for Y (371-372); stage rect from `[data-founder-stage]` (352-362); `worldPerGrid = Math.min(scaleX, scaleY)` with STAGE_FILL (375-377); `maxAbsZ` loop over sA/sB only (382-387); `extentRef` (404-408) feeds bbox(). Camera NEVER touched — group transform only.\n\n(6) CLOCK. `morphRef` is a single scalar CLAMPED TO [0,1] (648, 659-663, 666) advanced at `delta / MORPH_DURATION` (162, 1.4 s). It is PER-LEG today and would become TOTAL if one 0..1 scalar covered two legs — halving each leg to 0.7 s. `store.morphTarget` is typed `0 | 1`, `stage` is `'A'|'morphing'|'B'` (foundersMorphStore.ts:63, 81, 106).\n\n(7) PER-PORTRAIT vs GLOBAL constants (80-164). GLOBAL, no per-portrait split needed: MAX_COUNT_BY_TIER, GRID_W/GRID_H, STAGE_FILL, Z_RELIEF_MAX_FRAC, DEFAULT_EMISSIVE, the whole SAMPLE_SPEC_BASE ink model (it measures its own backdrop from the frame's top corners, so a third studio headshot needs no profile), SPREAD_MAX/ORBIT_MAX/DOLLY/PARALLAX_MAX/REST_*/ENTRY_DURATION/CULL_PAD, and the sim params SPRING 52 / DAMPING 7.5 / MAX_SPEED 16 / TURB 9 / travelTint [0.16,2.4,3.0] / emissive 1.18. NOTHING needs to become per-portrait. The only genuinely per-portrait values are already derived per-image by the sampler: halfExtentX/Y, rgb, ink, z. Two GLOBAL constants need RETUNING (not splitting): MAX_COUNT_BY_TIER.full (the union grows) and possibly GRID_W/GRID_H.\n\n(8) DEV HANDLE (756-878) assumes 2 in: `getSampler()` (meanInkA/meanInkB, 4-element `halfExtent`), `getUniforms()` (no uMorph2), `setMorph(v)` (override clamped 0..1 at 648), `setStage(s: \"A\"|\"B\")` (830-833), `playMorph(dir)` (834-839, binary target).",
  "changes": [
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "841-870 (PortraitMorphOpts)",
      "current": "export interface PortraitMorphOpts {\n  /** count×3 LINEAR rgb for target A (index-matched to homeA). */\n  colorsA: Float32Array;\n  /** count×3 LINEAR rgb for target B (index-matched to homeB). */\n  colorsB: Float32Array;\n  ...\n  sizeA?: Float32Array;\n  sizeB?: Float32Array;",
      "change": "Add two OPTIONAL fields after colorsB / sizeB:\n  /** count×3 LINEAR rgb for target C (index-matched to homeC). Present ONLY on a real 3-target chain; absent → the exact 2-target graph. */\n  colorsC?: Float32Array;\n  /** count floats 0..1: per-particle tonal weight for target C. Requires sizeA+sizeB present. */\n  sizeC?: Float32Array;\nKeep them optional so the hero path and any 2-target caller emit a byte-identical graph.",
      "rationale": "Colour and ink currently stop at B. Without C buffers the third leg renders Mattia's positions in Michele's colours and Michele's ink — and ink drives size AND alpha, so cells that are subject only in Mattia get Discarded at line 1446."
    },
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "932-948",
      "current": "const hasPortrait = !!portrait;\n  const colorABuffer = portrait ? instancedArray(portrait.colorsA.slice(), \"vec3\") : null;\n  const colorBBuffer = portrait ? instancedArray(portrait.colorsB.slice(), \"vec3\") : null;\n  ...\n  const hasPortraitSize = !!portrait?.sizeA && !!portrait?.sizeB;\n  const sizeABuffer = hasPortraitSize ? instancedArray(portrait!.sizeA!.slice(), \"float\") : null;\n  const sizeBBuffer = hasPortraitSize ? instancedArray(portrait!.sizeB!.slice(), \"float\") : null;",
      "change": "Add:\n  const hasPortraitC = !!portrait?.colorsC;\n  const colorCBuffer = hasPortraitC ? instancedArray(portrait!.colorsC!.slice(), \"vec3\") : null;\n  const hasPortraitSizeC = hasPortraitSize && !!portrait?.sizeC;\n  const sizeCBuffer = hasPortraitSizeC ? instancedArray(portrait!.sizeC!.slice(), \"float\") : null;",
      "rationale": "Per-particle C colour/ink must live in storage buffers read via .toAttribute() in the render stage, same discipline as A/B (vec3 reads need trailing .xyz, \"float\" reads must NOT — see 939-941 and 1174)."
    },
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "1167-1181",
      "current": "const portraitMorphExpr = hasPortrait\n    ? smoothstep(\n        0.0,\n        1.0,\n        clamp(morphN.sub(hash(instanceIndex).mul(0.55)).div(0.45), 0.0, 1.0),\n      )\n    : null;\n  // `\"float\"` storage buffers are unpadded — NO `.xyz` on these reads.\n  const portraitInkExpr = hasPortraitSize\n    ? mix(\n        sizeABuffer!.toAttribute(),\n        sizeBBuffer!.toAttribute(),\n        portraitMorphExpr!,\n      )\n    : null;",
      "change": "Add a SECOND-LEG stagger expression mirroring the kernel's m2 EXACTLY, and chain the ink through it:\n  const portraitMorph2Expr = hasPortraitC\n    ? smoothstep(0.0, 1.0, clamp(morph2N.sub(hash(instanceIndex).mul(0.55)).div(0.45), 0.0, 1.0))\n    : null;\n  const portraitInkExpr = hasPortraitSize\n    ? (hasPortraitSizeC\n        ? mix(mix(sizeABuffer!.toAttribute(), sizeBBuffer!.toAttribute(), portraitMorphExpr!), sizeCBuffer!.toAttribute(), portraitMorph2Expr!)\n        : mix(sizeABuffer!.toAttribute(), sizeBBuffer!.toAttribute(), portraitMorphExpr!))\n    : null;\nKeep BOTH as self-contained EXPRESSIONS. Do NOT hoist into an outer `.toVar()` and `.assign()` inside the vertex Fn.",
      "rationale": "The blend must be CHAINED (mix(mix(A,B,m1),C,m2)) to match the kernel's target.assign(mix(target,hC,...)) at 1015, or colour/size travel a different path than position. The expression-not-var shape is load-bearing: the VaryingNode hazard at 1147-1166 means a varying built from an outer var carries that var's DECLARED INITIAL value forever — the bug that previously pinned vMorphColorF to 0."
    },
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "1337-1338",
      "current": "const vMorphColorF = hasPortrait ? varying(portraitMorphExpr!) : null;\n  const vInkF = hasPortraitSize ? varying(portraitInkExpr!) : null;",
      "change": "const vMorphColorF = hasPortrait ? varying(portraitMorphExpr!) : null;\n  const vMorphColor2F = hasPortraitC ? varying(portraitMorph2Expr!) : null;\n  const vInkF = hasPortraitSize ? varying(portraitInkExpr!) : null;",
      "rationale": "The fragment needs the second-leg blend scalar. vInkF automatically carries the 3-way chain because portraitInkExpr was rewritten in place; vSizePxF/portraitSizePxExpr (1235-1244) also reference portraitInkExpr, so disc size and the coverage compensation stay in sync for free."
    },
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "1371-1373",
      "current": "const cA = colorABuffer!.toAttribute().xyz;\n      const cB = colorBBuffer!.toAttribute().xyz;\n      const base = mix(cA, cB, vMorphColorF!).toVar();",
      "change": "const cA = colorABuffer!.toAttribute().xyz;\n      const cB = colorBBuffer!.toAttribute().xyz;\n      const base = mix(cA, cB, vMorphColorF!).toVar();\n      if (hasPortraitC) {\n        base.assign(mix(base, colorCBuffer!.toAttribute().xyz, vMorphColor2F!));\n      }\nThe `if` is a JS build-time boolean, so the 2-target graph emits nothing extra.",
      "rationale": "Chains the photographic colour B→C on the same staggered wave the kernel uses for the anchor. `.xyz` is MANDATORY on a \"vec3\" storage buffer (padded to 4 components) — see 1370 and 1263-1267."
    },
    {
      "file": "src/webgl/gpgpu/gpgpuNodeSim.ts",
      "lines": "818-840 (PortraitMorphOpts doc block) and 1228-1234 (EXACTNESS NOTE)",
      "current": "...for target A and target B; the render blends both A→B with the SAME per-particle stagger...\n  // EXACTNESS NOTE: omitting `sizeFD` here is exact — NOT an approximation —\n  // only because `uSizeComp`/`uSizeComp2`/`uSizeComp3` are hard-set to 1 on the\n  // portrait path (FounderPortraitMorph.tsx:492-494) and never animated.",
      "change": "Update the doc block to state the portrait path now supports an OPTIONAL third target (colorsC/sizeC) blended by uMorph2 with the same stagger, chained after the A→B blend. Fix the stale line reference in the EXACTNESS NOTE (it points at 492-494; the uSizeComp pins are at FounderPortraitMorph.tsx:509-511) and re-assert that uSizeComp2 must STAY 1 now that uMorph2 is actually animated.",
      "rationale": "uMorph2 becoming live makes sizeFC (1295-1299) a real term — it mixes toward uSizeComp2 across smoothstep(0.25,0.75,morph2N). If uSizeComp2 were ever non-1, portraitSizePxExpr (which omits sizeFD) would desynchronise from sizeNode and the sub-pixel coverage term would start lying. It is pinned to 1 today so the change is safe, but the comment must be corrected rather than left pointing at wrong lines."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "100-111, 391-435",
      "current": "export interface PortraitPair {\n  a: PortraitPoints;\n  b: PortraitPoints;\n  ...\n}\n...\nexport function samplePortraitPair(imageA, imageB, spec) {\n  const readA = readGrid(imageA, spec);\n  const readB = readGrid(imageB, spec);\n  if (!readA || !readB) return null;\n  ...\n  for (let i = 0; i < total; i++) {\n    if (Math.max(readA.ink[i], readB.ink[i]) > spec.inkCut) hits.push(i);\n  }\n  ...\n  return { a: emit(readA, cells, spec), b: emit(readB, cells, spec), count, sharedCells, stride, gridW, gridH };",
      "change": "Generalize to N images. Preferred shape:\n  export interface PortraitSet { points: PortraitPoints[]; count: number; sharedCells: number; stride: number; gridW: number; gridH: number }\n  export function samplePortraitSet(images: HTMLImageElement[], spec: PortraitPairSpec): PortraitSet | null\nUnion becomes `let mx = 0; for (const rd of reads) mx = Math.max(mx, rd.ink[i]); if (mx > spec.inkCut) hits.push(i);` and the return maps `reads.map(rd => emit(rd, cells, spec))`. Keep `samplePortraitPair` as a thin wrapper (samplePortraitSet([a,b]) → { a: p[0], b: p[1], ... }) so no other caller breaks.\nIf the pair shape is kept and a `c` bolted on instead, the union loop and the emit list must STILL take all three.",
      "rationale": "Index j must be the SAME grid cell in all THREE images — that shared cell list is the whole pairing mechanism (comment at 407-409). A cell that is ink in Mattia but backdrop in both others must be in the list or Mattia permanently loses that part of his silhouette."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "166-168",
      "current": "/** Headshot asset discovery — preferred over the environmental fallback. */\nconst HEADSHOT_EXTS = [\"webp\", \"jpg\", \"png\"];\nconst FOUNDER_SLUGS = [\"alessandro\", \"michele\"];",
      "change": "const FOUNDER_SLUGS = [\"alessandro\", \"michele\", \"mattia\"];\nplus, alongside it:\n/** Number of morph targets in the chain (A→B→C). */\nconst TARGET_COUNT = FOUNDER_SLUGS.length;",
      "rationale": "public/founders/mattia-headshot.webp exists (74 KB) so the first ext attempt succeeds. loadFounder(2) is otherwise reached via `?? FOUNDER_SLUGS[0]` (line 220) and would silently sample Alessandro twice."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "219-229",
      "current": "async function loadFounder(idx: number): Promise<HTMLImageElement> {\n  const slug = FOUNDER_SLUGS[idx] ?? FOUNDER_SLUGS[0];\n  for (const ext of HEADSHOT_EXTS) {\n    try {\n      return await loadImg(`/founders/${slug}-headshot.${ext}`);\n    } catch { /* try next extension / fall back */ }\n  }\n  return loadImg(founders[idx]?.image ?? \"\");\n}",
      "change": "Keep the body, but harden the terminal fallback:\n  const fallback = founders[idx]?.image;\n  if (!fallback) throw new Error(`no portrait asset for founder ${idx}`);\n  return loadImg(fallback);\nand ensure src/data/founders.ts gains the Mattia Scattu entry with image: \"/founders/mattia-scattu.webp\".",
      "rationale": "loadImg(\"\") rejects, Promise.all rejects, and line 316's `.catch(() => {})` swallows it — producing a completely blank WebGL founders stage with no console error. Throwing surfaces the cause in dev; the data entry is the real fix (DOM card 03 needs it anyway)."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "240-243, 297-322",
      "current": "const imgARef = useRef<HTMLImageElement | null>(null);\n  const imgBRef = useRef<HTMLImageElement | null>(null);\n  const pairRef = useRef<PortraitPair | null>(null);\n...\n    void Promise.all([\n      loadFounder(0),\n      loadFounder(1),\n      import(\"./image/sampleImagePoints\"),\n    ])\n      .then(([a, b, mod]) => {\n        if (cancelled) return;\n        imgARef.current = a;\n        imgBRef.current = b;\n        sampleModRef.current = mod;\n        pairRef.current = mod.samplePortraitPair(a, b, sampleSpec());\n        setSampleEpoch((e) => e + 1);\n      })",
      "change": "Collapse the image refs into one array ref and load N:\n  const imgsRef = useRef<HTMLImageElement[]>([]);\n  const setRef = useRef<PortraitSet | null>(null);   // was pairRef\n...\n    void Promise.all([\n      Promise.all(FOUNDER_SLUGS.map((_, i) => loadFounder(i))),\n      import(\"./image/sampleImagePoints\"),\n    ])\n      .then(([imgs, mod]) => {\n        if (cancelled) return;\n        imgsRef.current = imgs;\n        sampleModRef.current = mod;\n        setRef.current = mod.samplePortraitSet(imgs, sampleSpec());\n        setSampleEpoch((e) => e + 1);\n      })\nRename pairRef → setRef at ALL five sites (334, 557-561, 762, 770, 843).",
      "rationale": "One sampler call must see all three images so they share one grid and one cell list. Two pair calls would produce two different cell lists and destroy index pairing."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "553-564 (resampleNowRef)",
      "current": "resampleNowRef.current = (opts) => {\n    const mod = sampleModRef.current;\n    const ia = imgARef.current;\n    const ib = imgBRef.current;\n    if (!mod || !ia || !ib) return;\n    tuningRef.current = { ...tuningRef.current, ...opts };\n    const next = mod.samplePortraitPair(ia, ib, sampleSpec());",
      "change": "const mod = sampleModRef.current;\n    const imgs = imgsRef.current;\n    if (!mod || imgs.length < FOUNDER_SLUGS.length) return;\n    tuningRef.current = { ...tuningRef.current, ...opts };\n    const next = mod.samplePortraitSet(imgs, sampleSpec());",
      "rationale": "Same generalization for the live dev resample; all three must be re-sampled by the one call or they drift onto different grids — the invariant the comment at 550-552 spells out."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "334-338",
      "current": "if (!pair || !webgpu || !tslNs || !mod) return;\n    const sA = pair.a;\n    const sB = pair.b;\n    // The instance count FOLLOWS the sampler (one particle per shared cell).\n    const count = pair.count;",
      "change": "if (!set || !webgpu || !tslNs || !mod) return;\n    const [sA, sB, sC] = set.points;\n    if (!sA || !sB || !sC) return;\n    const count = set.count;",
      "rationale": "Three per-image outputs, all index-paired on the one shared cell list."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "366-408",
      "current": "const halfX = Math.max(sA.halfExtentX, sB.halfExtentX, 1e-3);\n    const halfY = Math.max(sA.halfExtentY, sB.halfExtentY, 1e-3);\n    ...\n    let maxAbsZ = 1e-4;\n    for (let i = 0; i < count; i++) {\n      const za = Math.abs(sA.z[i]);\n      if (za > maxAbsZ) maxAbsZ = za;\n      const zb = Math.abs(sB.z[i]);\n      if (zb > maxAbsZ) maxAbsZ = zb;\n    }\n    ...\n    const homeA = toWorld(sA);\n    const homeB = toWorld(sB);",
      "change": "const pts = [sA, sB, sC];\n    const halfX = Math.max(...pts.map((p) => p.halfExtentX), 1e-3);\n    const halfY = Math.max(...pts.map((p) => p.halfExtentY), 1e-3);\n    ...\n    let maxAbsZ = 1e-4;\n    for (const p of pts)\n      for (let i = 0; i < count; i++) {\n        const z = Math.abs(p.z[i]);\n        if (z > maxAbsZ) maxAbsZ = z;\n      }\n    ...\n    const homeA = toWorld(sA);\n    const homeB = toWorld(sB);\n    const homeC = toWorld(sC);\nUpdate the comment at 368-370 (\"A & B SHARE ONE scale\") to say all THREE share one scale, fitting the LARGEST extent.",
      "rationale": "All three targets share one worldPerGrid (that is what keeps travel short and framing stable). Fitting the max of three keeps the widest/tallest face inside STAGE_FILL; the z-relief cap must consider C's relief or zNorm could exceed Z_RELIEF_MAX_FRAC on Mattia and reintroduce the comb tearing that constant exists to prevent (99-110)."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "417-435 (seed / keepEntry)",
      "current": "const keepEntry = preserveState && entryRef.current >= 1;\n    let seed: Float32Array;\n    if (keepEntry) {\n      seed = (morphRef.current >= 0.5 ? homeB : homeA).slice();\n    } else { ... }",
      "change": "Snap to the NEAREST of the three stages on a live rebuild:\n      const p = morphRef.current;               // now 0..2\n      seed = (p >= 1.5 ? homeC : p >= 0.5 ? homeB : homeA).slice();\nThe scatter branch (else) is unchanged — it still seeds around homeA, which is correct (A is the entry stage).",
      "rationale": "A resize/measure rebuild while the user is parked on Mattia must seed at homeC, otherwise the cloud snaps back to Michele's silhouette and springs across."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "459-512 (build call + uniform init)",
      "current": "const built = mod.createTextMorphComputeBuild(\n      gl as never, webgpu as never, tslNs as never,\n      homeA,\n      homeB,\n      // Single A→B leg: homeC/homeD = homeB, uMorph2/uMorph3 never advanced.\n      homeB,\n      homeB,\n      count,\n      { SPRING: 52, DAMPING: 7.5, MAX_SPEED: 16, TURB: 9, POINT_SIZE: pointSize, POINT_ALPHA: 1.0, EMISSIVE: 1, COL_COLD: [1,1,1], COL_HOT: [0.4,1,1] },\n      seed,\n      {\n        colorsA: sA.rgb,\n        colorsB: sB.rgb,\n        sizeA: sA.ink,\n        sizeB: sB.ink,\n        blending: \"normal\", depthTest: false, depthWrite: false,\n        emissive: emissiveRef.current,\n        travelTint: [0.16, 2.4, 3.0],\n        spacingDev,\n      },\n    ) as unknown as MorphBuild;\n\n    built.uMorph2.value = 0;\n    built.uMorph3.value = 0;",
      "change": "Pass a REAL third target (homeD = homeC so the unused fourth leg is a true identity mix), plus C colour/ink:\n      homeA,\n      homeB,\n      homeC,\n      homeC,\n      ...sim params unchanged...\n      {\n        colorsA: sA.rgb,\n        colorsB: sB.rgb,\n        colorsC: sC.rgb,\n        sizeA: sA.ink,\n        sizeB: sB.ink,\n        sizeC: sC.ink,\n        ...rest unchanged...\n      }\nKeep `built.uMorph3.value = 0;`. Replace the two `built.uMorph.value = morphRef.current` writes (518, 525) with one helper used by all three branches:\n    const applyMorph = (p: number) => {\n      built.uMorph.value = THREE.MathUtils.clamp(p, 0, 1);\n      built.uMorph2.value = THREE.MathUtils.clamp(p - 1, 0, 1);\n    };\n    keepEntry:       built.uAssemble.value = 1; applyMorph(morphRef.current); entryRef.current = 1;\n    preserveState:   built.uAssemble.value = entryRef.current; applyMorph(morphRef.current);\n    fresh:           built.uAssemble.value = 0; applyMorph(0); morphRef.current = 0; (rest of 526-537 unchanged)\nLeave built.uSizeComp / uSizeComp2 / uSizeComp3 = 1 (509-511) EXACTLY as they are.",
      "rationale": "homeD = homeC keeps the inert third leg a true identity even if uMorph3 ever drifted. Deriving uMorph/uMorph2 from one progress scalar guarantees uMorph is EXACTLY 1 before uMorph2 leaves 0 — the precondition for the chained mix (1010-1015) to describe a genuine B→C path rather than a shortcut."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "161-163 and 640-675 (one-shot clock + flight envelope)",
      "current": "/** One-shot A→B (or B→A) auto-play duration (seconds). Mirrors the hero morph. */\nconst MORPH_DURATION = 1.4;\n...\n    const override = morphOverrideRef.current;\n    if (override != null) {\n      morphRef.current = THREE.MathUtils.clamp(override, 0, 1);\n      if (store.morphImmediate) store.setMorphImmediate(false);\n    } else {\n      if (store.morphImmediate) { morphRef.current = store.morphTarget; store.setMorphImmediate(false); }\n      const target = store.morphTarget;\n      const cur = morphRef.current;\n      if (cur !== target) {\n        const dir = target > cur ? 1 : -1;\n        morphRef.current = THREE.MathUtils.clamp(cur + (dir * delta) / MORPH_DURATION, 0, 1);\n      }\n    }\n    const gc = THREE.MathUtils.clamp(morphRef.current, 0, 1);\n    b.uMorph.value = gc;\n    if (Math.abs(store.morph - gc) > 1e-4) store.setMorph(gc);\n    const nextStage = gc <= 0.02 ? \"A\" : gc >= 0.98 ? \"B\" : \"morphing\";\n    if (store.stage !== nextStage) store.setStage(nextStage);\n\n    const env = Math.sin(gc * Math.PI);",
      "change": "Promote morphRef to a PROGRESS scalar in [0, TARGET_COUNT-1] = [0,2]; MORPH_DURATION stays PER-LEG (one unit of p == one leg):\n    const MAX_P = TARGET_COUNT - 1;   // 2\n    const override = morphOverrideRef.current;\n    if (override != null) {\n      morphRef.current = THREE.MathUtils.clamp(override, 0, MAX_P);\n      if (store.morphImmediate) store.setMorphImmediate(false);\n    } else {\n      if (store.morphImmediate) { morphRef.current = store.morphTarget; store.setMorphImmediate(false); }\n      const target = store.morphTarget;              // now 0 | 1 | 2\n      const cur = morphRef.current;\n      if (cur !== target) {\n        const dir = target > cur ? 1 : -1;\n        morphRef.current = THREE.MathUtils.clamp(cur + (dir * delta) / MORPH_DURATION, 0, MAX_P);\n      }\n    }\n    const p = THREE.MathUtils.clamp(morphRef.current, 0, MAX_P);\n    b.uMorph.value = THREE.MathUtils.clamp(p, 0, 1);\n    b.uMorph2.value = THREE.MathUtils.clamp(p - 1, 0, 1);\n    if (Math.abs(store.morph - p) > 1e-4) store.setMorph(p);\n    const nextStage = p <= 0.02 ? \"A\" : Math.abs(p - 1) <= 0.02 ? \"B\" : p >= MAX_P - 0.02 ? \"C\" : \"morphing\";\n    if (store.stage !== nextStage) store.setStage(nextStage);\n\n    // Flight envelope: EXACTLY 0 at every locked stage (p = 0, 1, 2), peaking mid-leg.\n    const legT = p <= 1 ? p : p - 1;\n    const env = Math.sin(legT * Math.PI);\nEverything downstream of env (uSpread 675, dolly 701, yaw/pitch 724-729, restEnv/breath 723/746) is then automatically correct and needs NO edit.",
      "rationale": "A single 0..1 scalar spanning two legs would (a) halve each leg to 0.7 s and (b) make env = sin(p·π) equal 1 at the MIDDLE stage — maximum spread, dolly and orbit, zero rest-idle — exactly while Michele is supposed to be locked and crisp. The [0,2] progress with a leg-local envelope is the only shape that preserves the 'locked, crisp face' contract at all three stages."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "63, 80-85, 106, 118-132 (+ header doc 10-23)",
      "current": "export type FounderStage = \"A\" | \"morphing\" | \"B\";\n  /** uMorph the island animates toward (0 = Alessandro, 1 = Michele). */\n  morphTarget: 0 | 1;\n  /** Live uMorph 0..1 (writer: island; reader: DOM copy/poster cross-fade). */\n  morph: number;\n  setMorphTarget: (morphTarget: 0 | 1, immediate?: boolean) => void;\n  morphTarget: 0 as 0 | 1,",
      "change": "export type FounderStage = \"A\" | \"morphing\" | \"B\" | \"C\";\n  /** Progress the island animates toward: 0 = Alessandro, 1 = Michele, 2 = Mattia. */\n  morphTarget: 0 | 1 | 2;\n  /** Live morph PROGRESS 0..2 — leg-major (1.0 = target B fully formed). Writer: island; readers: DOM copy/poster cross-fade + the gate. */\n  morph: number;\n  setMorphTarget: (morphTarget: 0 | 1 | 2, immediate?: boolean) => void;\n  morphTarget: 0 as 0 | 1 | 2,\nAlso rewrite the header doc block (10-23) as A → B → C: two in-section gestures to advance, a third to release.",
      "rationale": "This is the contract the founders-rail gate and the DOM cross-fade read. Not my file, but the island's writes (setMorph 0..2, setStage 'C', setMorphTarget 2) are invalid against the current types and WILL fail typecheck until it changes — must land in the same commit."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "83-96 (count budget + grid)",
      "current": "const MAX_COUNT_BY_TIER: Record<\"full\" | \"lite\", number> = {\n  full: 48000,\n  lite: 16000,\n};\nconst GRID_W = 290;\nconst GRID_H = 405;",
      "change": "Raise the full ceiling so the three-way union does not trip the integer-stride cliff:\n  full: 64000,\n  lite: 16000,\nand update the GRID comment: 290×405 = 117,450 grid cells; the TWO-portrait union measured 42,087 shared cells. A third portrait can only GROW the union (union is monotone in image count), plausibly to ~48-56k, which under the old 48,000 ceiling forces stride 2 and HALVES the count to ~24-28k. Verify with __sersanFounderMorph.getSampler() (sharedCells + stride), then decide between keeping 290×405 at a 64k ceiling or shrinking the grid by scale = sqrt(42087 / measuredUnion).",
      "rationale": "`stride = sharedCells > maxCount ? Math.ceil(sharedCells/maxCount) : 1` (sampleImagePoints.ts:420-421) is an integer cliff — one cell over the ceiling halves the cloud. Most likely silent visual regression of the change. The count also feeds spacingDev/defPointSize (444-457), so a halved count auto-grows the discs: the failure looks like 'blurry/chunky faces', not 'sparse'."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "769-788 (getSampler)",
      "current": "getSampler() {\n        const p = pairRef.current;\n        if (!p) return null;\n        const meanInk = (s: PortraitPoints) => { let t = 0; for (let i = 0; i < p.count; i++) t += s.ink[i]; return t / Math.max(p.count, 1); };\n        return { gridW: p.gridW, gridH: p.gridH, sharedCells: p.sharedCells, stride: p.stride, count: p.count, maxCount,\n          meanInkA: meanInk(p.a),\n          meanInkB: meanInk(p.b),\n          halfExtent: [p.a.halfExtentX, p.a.halfExtentY, p.b.halfExtentX, p.b.halfExtentY],\n        };\n      },",
      "change": "const s = setRef.current;\n        if (!s) return null;\n        const meanInk = (pt: PortraitPoints) => { let t = 0; for (let i = 0; i < s.count; i++) t += pt.ink[i]; return t / Math.max(s.count, 1); };\n        return {\n          gridW: s.gridW, gridH: s.gridH, sharedCells: s.sharedCells, stride: s.stride, count: s.count, maxCount,\n          meanInk: s.points.map(meanInk),                        // [A, B, C]\n          halfExtent: s.points.map((pt) => [pt.halfExtentX, pt.halfExtentY]),\n        };",
      "rationale": "This readout is the ONLY way to verify the stride cliff and to confirm Mattia's ink curve matches the others (a mean ink far off A/B means his backdrop is being read differently and needs a resample, not a code change). Array-shaped so a fourth target needs no further edit."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "789-799 (getUniforms)",
      "current": "getUniforms() {\n        const bb = buildRef.current;\n        return {\n          uAssemble: bb?.uAssemble.value ?? 0,\n          uMorph: bb?.uMorph.value ?? 0,\n          uFade: bb?.uFade.value ?? 0,\n          uSpread: bb?.uSpread.value ?? 0,\n          emissive: bb?.uEmissive?.value ?? emissiveRef.current,\n          pointSize: bb?.uPointSize.value ?? 0,\n        };\n      },",
      "change": "Add, right after uMorph:\n          uMorph2: bb?.uMorph2.value ?? 0,\n          progress: morphRef.current,",
      "rationale": "Without uMorph2 exposed there is no way for QA to distinguish 'parked at B' from 'parked at C' — both report uMorph = 1."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "827-839 (setMorph / setStage / playMorph)",
      "current": "setMorph(v: number | null) { morphOverrideRef.current = v; },\n      setStage(s: \"A\" | \"B\") {\n        morphOverrideRef.current = null;\n        useFoundersMorphStore.getState().setMorphTarget(s === \"B\" ? 1 : 0, true);\n      },\n      playMorph(dir: number) {\n        morphOverrideRef.current = null;\n        useFoundersMorphStore.getState().setMorphTarget(dir >= 0 ? 1 : 0, false);\n      },",
      "change": "setMorph(v: number | null) { morphOverrideRef.current = v; },   // doc: 0..2 now\n      setStage(s: \"A\" | \"B\" | \"C\") {\n        morphOverrideRef.current = null;\n        const t = s === \"C\" ? 2 : s === \"B\" ? 1 : 0;\n        useFoundersMorphStore.getState().setMorphTarget(t as 0 | 1 | 2, true);\n      },\n      /** Advance/retreat exactly ONE leg from the current progress. */\n      playMorph(dir: number) {\n        morphOverrideRef.current = null;\n        const cur = Math.round(morphRef.current);\n        const t = THREE.MathUtils.clamp(cur + (dir >= 0 ? 1 : -1), 0, TARGET_COUNT - 1);\n        useFoundersMorphStore.getState().setMorphTarget(t as 0 | 1 | 2, false);\n      },",
      "rationale": "playMorph(+1) from B must reach C; the current binary form makes the third stage unreachable from the dev handle, which would block the whole QA loop. setMorph's clamp is applied at the consumption site (frame loop), already widened to 0..MAX_P."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "3-62 (module doc block), specifically 12-17 and 43-45",
      "current": "One scroll-down gesture triggers a ONE-SHOT auto-play A→B that\n * runs to completion on its own clock (MORPH_DURATION); at B the cloud LOCKS;\n * another gesture releases the page.\n...\n * ENGINE REUSE. createTextMorphComputeBuild (gpgpu/gpgpuNodeSim.ts) with the\n * OPTIONAL `portrait` param. homeC/homeD = homeB, uMorph2/uMorph3 stay 0 (single\n * A→B leg). With `portrait` undefined the build is byte-identical to the hero.",
      "change": "Rewrite both passages for the three-target chain: gestures play A→B then B→C, each on its own MORPH_DURATION clock, with a lock at each of the three stages and release after C (symmetric on scroll-up). And: 'homeC = the third portrait (Mattia) with real colorsC/sizeC; homeD = homeC so the fourth leg is an identity mix and uMorph3 stays 0. The morph clock is a PROGRESS scalar in [0,2] — uMorph = clamp(p,0,1), uMorph2 = clamp(p-1,0,1) — which guarantees leg 1 completes exactly before leg 2 opens, the precondition for the kernel's CHAINED target blend.'",
      "rationale": "This file's doc block is the primary map for the next reader; leaving it claiming a single A→B leg would actively mislead about the uMorph/uMorph2 sequencing invariant, which is the subtlest part of the change."
    }
  ],
  "hazards": [
    "*** HIGHEST RISK, CONFIRMED *** Position blending IS chained (gpgpuNodeSim.ts:1010-1020, `target.assign(mix(target, hC, ...))`), so uMorph2 correctly travels B→C. But COLOUR and INK are NOT: portraitMorphExpr (1167-1173) and portraitInkExpr (1175-1181) read `morphN` only, and the fragment does `mix(cA, cB, vMorphColorF)` (1373). Naive wiring (real homeC, uMorph2 animated, no colorsC/sizeC) renders Mattia's POSITIONS with Michele's COLOURS and Michele's INK — and ink gates disc size AND alpha with a Discard at alpha < 0.02 (1446), so cells that are subject in Mattia but backdrop in Michele are culled entirely. Mattia would appear as a Michele-shaped hole. The shader extension is MANDATORY.",
    "THE STRIDE CLIFF. sampleImagePoints.ts:420-421: `stride = sharedCells > maxCount ? Math.ceil(sharedCells/maxCount) : 1`. The shared-cell list is the UNION of inks and a three-image union is strictly ≥ the two-image one (measured 42,087 against a 48,000 ceiling — only 14% headroom). One cell over and stride jumps to 2, halving the cloud to ~24k. It will not throw; it will just look wrong. Measure with getSampler() before and after and raise MAX_COUNT_BY_TIER.full.",
    "THE ENVELOPE AT THE MIDDLE STAGE. `env = Math.sin(gc * Math.PI)` (674) drives uSpread, dolly, orbit yaw AND (via restEnv = 1 - env, 723) the rest-idle sway and breath. Normalizing two legs into one 0..1 makes env = 1 at Michele (p = 0.5): maximum spread, dolly and orbit, zero rest-idle — the exact opposite of the 'locked, crisp face' contract, while the DOM copy claims stage B. The leg-local fraction is not a nicety.",
    "MORPH_DURATION (1.4 s, line 162) is PER LEG today because the clock advances one unit of a 0..1 range. One 0..1 range spanning two legs silently makes it TOTAL and each leg 0.7 s. The [0,2] progress preserves the shipped feel exactly.",
    "VaryingNode hazard (gpgpuNodeSim.ts:1105-1119, 1147-1166): `varying(node)` re-emits its node at the TOP of vertex main(), before material.vertexNode runs. portraitMorph2Expr and the rewritten portraitInkExpr MUST stay self-contained expressions. Hoisting either into an outer `.toVar()` + `.assign()` inside the Fn silently pins the varying to its declared initial value — the class of bug that previously made vMorphColorF read a constant 0 (the face never left target A).",
    "Buffer swizzle discipline: `\"vec3\"` storage buffers pad to 16 bytes, so `colorCBuffer.toAttribute()` yields FOUR components and the trailing `.xyz` is MANDATORY (1263-1267, 1370). `\"float\"` buffers (sizeCBuffer) are unpadded and must NOT get `.xyz` (939-941, 1174). Getting this backwards throws or truncates at shader build time.",
    "uSizeComp2 (1295-1299) becomes a LIVE term the moment uMorph2 animates — sizeFC mixes toward it across smoothstep(0.25,0.75,morph2N). It is pinned to 1 at FounderPortraitMorph.tsx:509-511, keeping it an identity, but portraitSizePxExpr (1235-1244) deliberately OMITS sizeFD and the EXACTNESS NOTE (1228-1234) says that is exact only while those uniforms are 1. Do not animate them; and note that note's line reference (492-494) is already stale.",
    "loadImg(\"\") rejects and the load effect's `.catch(() => {})` (316) swallows it — a missing FOUNDER_SLUGS[2] asset or a missing founders[2] data entry yields a completely blank WebGL founders stage with NO console error. The public assets exist (mattia-headshot.webp 74 KB, mattia-scattu.webp 40 KB) but src/data/founders.ts still has only two entries (39-111).",
    "pairRef is read in FIVE places (334, 557-561, 762, 770, 843). A partial rename to setRef leaves a stale pair.a/pair.b read. Rename all in one pass.",
    "The delay/stagger buffer (gpgpuNodeSim.ts:952-967) derives its left→right entry wave from homeA's x span only. That remains correct for a 3-target chain (the entry is always into A) — do not 'fix' it.",
    "The dev-handle block (756-878) runs on EVERY render, outside any effect, and closes over maxCount and the refs. The array-shaped getSampler is fine; do not move per-frame state into it (island rule).",
    "Store type widening (morphTarget 0|1 → 0|1|2, FounderStage + 'C', morph 0..1 → 0..2) is a cross-file contract change with founders-rail.tsx (gate state machine + DOM copy cross-fade). The island cannot compile against the current store types — it must land in the same commit or the build breaks.",
    "Rebuilds are frequent: the build effect (567-597) re-runs on gl / maxCount / sampleEpoch / measureVersion / size.width, and measureVersion bumps on mount, intro-complete, fonts-ready and every resize. The keepEntry logic (417) is what keeps that from consuming the entry. The three-way seed selection must preserve that behaviour exactly, or a resize mid-section will replay the entrance."
  ],
  "openQuestions": [
    "Grid/budget decision: keep GRID_W/GRID_H at 290×405 and raise MAX_COUNT_BY_TIER.full to 64000, or hold the 48k ceiling and shrink the grid by sqrt(42087/measuredUnion)? Undecidable until __sersanFounderMorph.getSampler() is run with the third image in the union — the three-way sharedCells figure is unknown. Recommendation: raise the ceiling first (safe, monotone), measure, then decide whether to retarget the grid for GPU cost.",
    "Does the founders-rail gate want THREE stages with two in-section gestures and a third to release (A →g B →g C →g release), or a shortened hold at B? The island is agnostic — it only needs store.morphTarget ∈ {0,1,2} — but the scroll-up release semantics (C → B → A → release) are the rail's to define, and the island's stage thresholds (p ≤ 0.02 / |p-1| ≤ 0.02 / p ≥ 1.98) assume all three are genuine locked holds.",
    "Keep PortraitPair (with a bolted-on `c`) or replace it with an N-ary PortraitSet? I recommend PortraitSet plus a thin samplePortraitPair wrapper, but I did not audit every caller of samplePortraitPair outside this island — grep before removing the pair export.",
    "Is Mattia's headshot backdrop the same studio-white as the other two? The ink model measures its own backdrop from the frame's TOP CORNERS and removes it via a border-seeded flood fill (BG_FILL_TOL 0.055, BG_FILL_ROW_LIMIT 0.62), so a different surround should be handled by the same path — but this pipeline has been bitten by white studio backgrounds before. Verify meanInk[2] lands near meanInk[0..1] in getSampler() before assuming no sampler tuning is needed, and do NOT reintroduce a luminance gate (SAMPLE_SPEC_BASE comment 128-133 is explicit).",
    "MORPH_DURATION: with three targets the total pinned dwell roughly doubles. Is 1.4 s per leg still the desired feel across two legs, or should it drop to ~1.15 s to keep the total scroll-jack hold comparable? Product call, not a technical constraint."
  ]
}