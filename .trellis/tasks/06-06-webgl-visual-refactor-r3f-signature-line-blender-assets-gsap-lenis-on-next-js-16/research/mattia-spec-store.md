{
  "subsystem": "Home founders scroll-jack gate — C:\\Users\\alber\\Desktop\\sersan-v2-main\\src\\components\\sections\\founders-rail.tsx (1702 lines), with the contract it shares with src\\webgl\\store\\foundersMorphStore.ts and src\\webgl\\FounderPortraitMorph.tsx",
  "summary": "The two-target assumption is encoded in FIVE places, and NOT in the scroll length.\n\n(1) SCALAR MORPH SPACE. `store.morph` is a single 0..1 uMorph; `store.morphTarget` is typed literally `0 | 1` (foundersMorphStore.ts:81,106,123,143); the island derives `stage` as `gc <= 0.02 ? \"A\" : gc >= 0.98 ? \"B\" : \"morphing\"` (FounderPortraitMorph.tsx:670). Every m-keyed constant in founders-rail (COPY_EXIT_START/END 0.02/0.3, COPY_ENTER_START/END 0.7/0.98, the poster smoothstep(0.35,0.65), the counter's `m >= 0.5`, the hairline scaleX = m) reads that 0..1 as \"the whole section\".\n\n(2) TWO NAMED DOM SLOTS. copyARef/copyBRef and stageImgARef/stageImgBRef (lines 556-559), hand-built writers setOpA/setYA + bFx (609-642), two `lastAHidden`/`lastBHidden` latches (729-730), founders[0]/founders[1] hardcoded in the JSX (1568,1580,1594,1606), and a teardown that poses exactly two blocks (1243-1253).\n\n(3) STAGE-LETTER BRANCHING IN step() (923-939). `dir > 0`: `stage === \"A\"` -> morph to 1 and stay engaged; `stage === \"B\"` -> release(1). `dir < 0`: `stage === \"B\"` -> morph to 0; `stage === \"A\"` -> release(-1). The rule is \"near end morphs, FAR end releases\" — with two stages, \"far end for down\" and \"near end for up\" happen to be the same node (B), which is why the letters work. With three, A and C are the only release nodes and B must morph in BOTH directions. Release therefore has to move from a letter test to an index-boundary test.\n\n(4) ENTRY LOCK. engage() takes `initStage: \"A\" | \"B\"` (855) and does `s.setMorphTarget(initStage === \"B\" ? 1 : 0, true)` (870); tick calls `engage(fromBottom ? \"B\" : \"A\")` (1148) and `engage(\"A\")` (1150).\n\n(5) GATE CHROME literals \"01\" (1634) and \"/ 02\" (1637), plus the counter flip at 775 and the poster cross-fade at 757.\n\nCRITICAL — PIN LENGTH: the pinned window needs ZERO extra scroll length. This is a GATE model, not a scrub. measure() (783-790) sets `section.style.height = innerHeight` and `store.setLayout(0, secTop)` — travel is literally 0, and the comment at 784-785 says so explicitly. The hold is produced by `lenis.stop()` + the per-frame re-snap in tick() (1097-1121), not by a tall runway. A third stage costs nothing in px. What MUST grow is the TIME budget: G_MAX_ENGAGE_MS = 16000 (line 88) is the force-release safety valve. It currently covers 1 leg (MORPH_DURATION 1.4s) + entry lock + G_IDLE_MS re-arms + human dwell. Two legs needs it raised to ~24000 (see change spec) or a long-dwelling user gets ejected at Mattia's leg. Everything else — section height, secTop, the sticky h-screen frame, `roomy` — is unchanged.\n\nThe horizontal rail, the native scroller, FounderPanel and FounderCopy all ALREADY generalise: `panels()` maps `founders` (1470-1474), `total = founders.length` (1429), the rail's fx array is built by querying `[data-founders-panel]` (1305-1325) and its travel is `track.scrollWidth - vw` (1329). Adding a third entry to src/data/founders.ts makes all three of those correct with no code change. Only the MORPH branch hardcodes 2.",
  "changes": [
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "63, 81, 106, 123, 143 (+ new field)",
      "current": "export type FounderStage = \"A\" | \"morphing\" | \"B\";\n...\n  /** uMorph the island animates toward (0 = Alessandro, 1 = Michele). */\n  morphTarget: 0 | 1;\n...\n  setMorphTarget: (morphTarget: 0 | 1, immediate?: boolean) => void;\n...\n  morphTarget: 0 as 0 | 1,",
      "change": "Widen the morph space to GLOBAL leg coordinates [0, N-1] (N = founders.length):\n\n  export type FounderStage = \"A\" | \"morphing\" | \"B\" | \"C\";\n  /** uMorph the island animates toward, in LEG units: 0=A, 1=B, 2=C. */\n  morphTarget: number;\n  /** Index of the LOCKED stage (0..N-1), or -1 while morphing. Island-derived. */\n  stageIndex: number;\n  setMorphTarget: (morphTarget: number, immediate?: boolean) => void;\n  setStageIndex: (stageIndex: number) => void;\n\nINITIAL gains `stageIndex: 0`, morphTarget stays 0. `morph` stays the live value but now spans 0..N-1. Keep the letter `stage` for the island's dev hook and for the `=== \"morphing\"` tests the gate already does; add stageIndex as the single numeric source of truth so the gate never re-derives it.",
      "rationale": "The gate must be able to say \"advance one leg\" / \"am I at an interior node\" without letters. Global leg coordinates (rather than leg-index + leg-local 0..1) keep EVERY existing copy/poster/chrome curve a pure function of one scalar m, which is what makes the reverse legs mirror for free (file comment 717-721). Leg-local coords would require a second variable in every one of those pure functions and would break reversal at the leg seam."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "36-51 (header block, mode 1)",
      "current": "     *      stage. The WebGL particle-portrait morph island (webgl/FounderPortraitMorph)\n     *      renders a ~26k-point cloud over [data-founder-stage] that composes founder\n     *      A, decomposes into a swarm at mid-scroll, and recomposes founder B; the two\n     *      DOM copy blocks hand off in lockstep with the island's live uMorph —\n     *      A's block exits early in the leg, the swarm owns the stage alone\n     *      through the middle, B's copy arrives child-by-child late —",
      "change": "Rewrite for N stages: \"...composes founder 0, decomposes into a swarm, recomposes founder 1, and on a second gesture repeats the leg into founder 2; the N DOM copy blocks hand off in lockstep with the island's live uMorph, which now spans [0, N-1] in LEG units. Per leg the outgoing block exits early, the swarm owns the stage alone through the middle, the incoming block arrives child-by-child late. The gate counter reads 01/03 -> 02/03 -> 03/03 and the hairline tracks m/(N-1).\" Also update line 1616's chrome comment (\"stage counter (01/02 -> 02/02)\").",
      "rationale": "This header is the file's contract doc and is explicitly two-founder. Leaving it stale is how the next reader re-introduces the hardcode."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "87-88",
      "current": "/** Max time (ms) the gate may hold the page before force-releasing (safety). */\nconst G_MAX_ENGAGE_MS = 16000;",
      "change": "/** Max time (ms) the gate may hold the page before force-releasing (safety).\n * Scales with the number of LEGS: each leg costs MORPH_DURATION (1.4s) + a\n * G_IDLE_MS re-arm + human dwell before the next gesture. 16s covered one leg;\n * two legs plus the entry lock need ~24s or a deliberate reader is ejected\n * mid-sequence by the safety valve. */\nconst G_MAX_ENGAGE_MS = 8000 + 8000 * (founders.length - 1);   // 24000 at N=3",
      "rationale": "THE one place where a third stage genuinely needs a longer window. The scroll runway does not grow (travel is 0); the HOLD DURATION does. At N=3 a user who pauses to read Michele before flicking to Mattia can easily exceed 16s of engaged time and be force-released at line 1121 mid-section."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "92-113",
      "current": "// --- MORPH-mode copy handoff (pure functions of the island's live uMorph, so\n// reverse legs mirror automatically) -----------------------------------------\n/** Founder A's copy EXITS over m∈[start,end] ...\nconst COPY_EXIT_START = 0.02;\nconst COPY_EXIT_END = 0.3;\n...\nconst COPY_ENTER_START = 0.7;\nconst COPY_ENTER_END = 0.98;",
      "change": "Keep the five numeric values EXACTLY as they are, but re-document them as LEG-LOCAL: \"all COPY_* windows are expressed in leg-local units u ∈ [0,1] measured from the block's own index; applyStage converts the global m to u per block. Founder i exits over m ∈ [i+EXIT_START, i+EXIT_END] and enters over m ∈ [i-1+ENTER_START, i-1+ENTER_END].\" ENTER_END = 0.98 keeps matching the island's per-leg lock threshold.",
      "rationale": "The numbers are already correct per leg; only their frame of reference changes. Preserving the exact values guarantees the A->B leg is byte-identical to today's, so the change is provably additive for the existing pair."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "556-559",
      "current": "  const copyARef = useRef<HTMLDivElement | null>(null);\n  const copyBRef = useRef<HTMLDivElement | null>(null);\n  const stageImgARef = useRef<HTMLImageElement | null>(null);\n  const stageImgBRef = useRef<HTMLImageElement | null>(null);",
      "change": "  const copyRefs = useRef<(HTMLDivElement | null)[]>([]);\n  const stageImgRefs = useRef<(HTMLImageElement | null)[]>([]);\n\nJSX assigns with `ref={(el) => { copyRefs.current[i] = el; }}` inside the map. Do NOT switch to querySelectorAll for these — the gate effect reads them synchronously at line 598-604 and refs are already committed by then; a query would also be order-fragile against the poster imgs which share [data-founder-media].",
      "rationale": "Two named slots is assumption (2). Arrays indexed by founder are the only shape that lets applyStage stay a single loop."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "596-642 (writer construction)",
      "current": "    const setImgA = stageImgARef.current ? (gsap.quickSetter(...)) : null;\n    const setImgB = ...;\n    let setOpA ...; let setYA ...;\n    if (copyA) { gsap.set(copyA, { opacity: 1, y: 0 }); ... }\n    const bChildren = copyB ? Array.from(copyB.querySelectorAll<HTMLElement>(\":scope > div > *\")) : [];\n    const childDur = Math.max(0.06, COPY_ENTER_END - COPY_ENTER_START - (bChildren.length - 1) * COPY_ENTER_STAGGER);\n    const bFx = bChildren.map((el, i) => { gsap.set(el, { opacity: 0, y: COPY_ENTER_Y }); return { start: COPY_ENTER_START + i * COPY_ENTER_STAGGER, setO: ..., setY: ... }; });\n    if (copyB) gsap.set(copyB, { opacity: 1 });",
      "change": "Build ONE uniform per-founder bundle. Every block gets BOTH a block-level exit writer AND per-child enter writers (block 0's enter simply always evaluates to 1, block N-1's exit always evaluates to 0 — they fall out of the math, no special-casing):\n\n    const N = founders.length;\n    type BlockFx = {\n      el: HTMLElement;\n      setOp: (v: number) => void;   // block opacity (exit)\n      setY: (v: number) => void;    // block y (exit)\n      children: { el: HTMLElement; start: number; dur: number; setO: (v:number)=>void; setY: (v:number)=>void }[];\n      lastHidden: boolean | null;\n    };\n    const blocks: BlockFx[] = [];\n    copyRefs.current.slice(0, N).forEach((el, i) => {\n      if (!el) return;\n      gsap.set(el, { opacity: 1, y: 0 });\n      const kids = Array.from(el.querySelectorAll<HTMLElement>(\":scope > div > *\"));\n      // dur is PER BLOCK: Mattia has no previouslyAt row, so his child count\n      // differs and a shared childDur would push his last child past\n      // COPY_ENTER_END (the island's lock threshold) or run it short.\n      const dur = Math.max(0.06, COPY_ENTER_END - COPY_ENTER_START - (kids.length - 1) * COPY_ENTER_STAGGER);\n      const children = kids.map((k, j) => {\n        gsap.set(k, i === 0 ? { opacity: 1, y: 0 } : { opacity: 0, y: COPY_ENTER_Y });\n        return { el: k, start: COPY_ENTER_START + j * COPY_ENTER_STAGGER, dur, setO: gsap.quickSetter(k, \"opacity\") as (v:number)=>void, setY: gsap.quickSetter(k, \"y\", \"px\") as (v:number)=>void };\n      });\n      blocks[i] = { el, setOp: gsap.quickSetter(el, \"opacity\") as (v:number)=>void, setY: gsap.quickSetter(el, \"y\", \"px\") as (v:number)=>void, children, lastHidden: null };\n    });\n    const setImgs = stageImgRefs.current.slice(0, N).map((el) => el ? (gsap.quickSetter(el, \"opacity\") as (v:number)=>void) : null);",
      "rationale": "Unifies the two divergent treatments (A = block-level, B = per-child) so the loop is index-agnostic. Block opacity multiplies child opacity in the compositor, so exit-on-the-block and enter-on-the-children compose correctly without fighting. The per-block dur is a real bug the naive port would introduce: line 628-633 computes childDur ONCE from B's child list, and Mattia (no `previouslyAt`) will have 5 children where Michele has 6."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "728-781 (applyStage, incl. poster 757 and counter 774-780)",
      "current": "    let posterShown = false;\n    let lastAHidden: boolean | null = null;\n    let lastBHidden: boolean | null = null;\n    const applyStage = (m: number) => {\n      const exitT = smoothstep(COPY_EXIT_START, COPY_EXIT_END, m);\n      setOpA?.(1 - exitT);\n      setYA?.(-COPY_EXIT_Y * exitT);\n      for (const c of bFx) { const e = smoothstep(c.start, c.start + childDur, m); c.setO(e); c.setY(COPY_ENTER_Y * (1 - e)); }\n      const aHidden = exitT >= 1;\n      const bHidden = m <= COPY_ENTER_START;\n      ...\n      const bF = smoothstep(0.35, 0.65, m);\n      if (useFoundersMorphStore.getState().active || !posterShown) { setImgA?.(0); setImgB?.(0); }\n      else { setImgA?.(1 - bF); setImgB?.(bF); }\n      if (setChromeLine) { const q = Math.round(m * 512) / 512; ... setChromeLine(q); }\n      if (chromeCounterEl) { const label = m >= 0.5 ? \"02\" : \"01\"; ... }\n    };",
      "change": "    let posterShown = false;\n    const applyStage = (m: number) => {\n      const live = useFoundersMorphStore.getState();\n      const hidePosters = live.active || !posterShown;\n      for (let i = 0; i < N; i++) {\n        const local = m - i;              // >0 while this block is departing\n        const u = local + 1;              // 0..1 across the leg that BRINGS it in\n        const exitT = smoothstep(COPY_EXIT_START, COPY_EXIT_END, local);\n        const b = blocks[i];\n        if (b) {\n          b.setOp(1 - exitT);\n          b.setY(-COPY_EXIT_Y * exitT);\n          for (const c of b.children) {\n            const e = smoothstep(c.start, c.start + c.dur, u);\n            c.setO(e); c.setY(COPY_ENTER_Y * (1 - e));\n          }\n          // Hidden when fully exited OR not yet begun entering — an invisible\n          // LinkedIn link must never take a click or a Tab stop (contract 740-745).\n          const hidden = exitT >= 1 || u <= COPY_ENTER_START;\n          if (hidden !== b.lastHidden) { b.lastHidden = hidden; b.el.style.visibility = hidden ? \"hidden\" : \"\"; }\n        }\n        // Poster i = (entering weight) x (1 - exiting weight): both legs use the\n        // SAME 0.35/0.65 window, so at any m the visible posters sum to 1 and the\n        // seam at integer m is exactly one poster at 1 — no flash, no dip.\n        const set = setImgs[i];\n        if (set) set(hidePosters ? 0 : smoothstep(0.35, 0.65, u) * (1 - smoothstep(0.35, 0.65, local)));\n      }\n      if (setChromeLine) {\n        const q = Math.round((m / Math.max(1, N - 1)) * 512) / 512;   // hairline spans the WHOLE sequence\n        if (q !== lastLineQ) { lastLineQ = q; setChromeLine(q); }\n      }\n      if (chromeCounterEl) {\n        const label = String(Math.min(N, Math.round(m) + 1)).padStart(2, \"0\");\n        if (label !== lastCounter) { lastCounter = label; chromeCounterEl.textContent = label; }\n      }\n    };\n\nEdge behaviour, verified: for i=0, local = m >= 0 so u >= 1 > COPY_ENTER_END and every child's smoothstep saturates at 1 — block 0 is permanently \"entered\", exactly as today. For i=N-1, local <= 0 so exitT is 0 — it never exits. Both ends are free; no branches needed.",
      "rationale": "Every curve stays a pure function of the single scalar m, which is what makes reverse legs mirror without extra state (comment 717-721). `Math.round(m)+1` reproduces today's flip point precisely (m>=0.5 -> \"02\") and extends it (m>=1.5 -> \"03\"). Multiplying the two poster smoothsteps is the only formulation that avoids both a double-bright seam at m=1 and a dip toward 0 between legs."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "783-790 (measure) and 1194-1199 (resize handler)",
      "current": "    const measure = () => {\n      // No travel (gate model): the section is exactly one viewport tall — the\n      // gate holds the page while a leg plays. secTop drives island placement.\n      section.style.height = `${window.innerHeight}px`;\n      const secTop = section.getBoundingClientRect().top + window.scrollY;\n      store.setLayout(0, secTop);\n      store.bumpMeasure();\n    };",
      "change": "NO CHANGE. Extend the comment only: \"...one viewport tall REGARDLESS of the number of stages — the runway is TIME (G_MAX_ENGAGE_MS), not px. Adding a stage adds zero scroll length.\" Same note for the resize handler at 1194-1199, which duplicates the innerHeight write.",
      "rationale": "This is the single most likely place for a wrong edit. Someone porting a scrubbed rail would multiply the height by the leg count; that would break the gate (tick's drift test at 1101-1120 re-snaps top to 0, so the extra height is dead space below the sticky) and would move the [data-line-anchor=\"founders\"] waypoint the header block at 46-50 warns about."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "855-894 (engage)",
      "current": "    const engage = (initStage: \"A\" | \"B\") => {\n      ...\n      s.setMorphTarget(initStage === \"B\" ? 1 : 0, true);",
      "change": "    const engage = (initIndex: number) => {          // 0 = first founder, N-1 = last\n      ...\n      s.setMorphTarget(initIndex, true);              // immediate lock at the ENTRY side\n\nEverything else in engage (armed=false, acc=0, the G_IDLE_MS re-arm, the lenis snap to secTop, showChrome) is index-agnostic and stays byte-identical.",
      "rationale": "Entering from below must land on the LAST founder (Mattia), not on Michele. `initStage === \"B\" ? 1 : 0` silently means \"the last one\" today only because N=2."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1147-1151 (tick engage calls)",
      "current": "          if ((fromTop || fromBottom) && rect.bottom > 0 && top < ihNow) {\n            engage(fromBottom ? \"B\" : \"A\");\n          } else if (inside) {\n            engage(\"A\"); // reload landed inside → pin without wedging\n          }",
      "change": "          if ((fromTop || fromBottom) && rect.bottom > 0 && top < ihNow) {\n            engage(fromBottom ? N - 1 : 0);\n          } else if (inside) {\n            engage(0); // reload landed inside → pin without wedging\n          }",
      "rationale": "Direct consequence of the engage() signature change; `N - 1` is the scroll-up entry node."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "920-939 (step — THE state machine)",
      "current": "    const step = (dir: number) => {\n      const s = useFoundersMorphStore.getState();\n      if (s.stage === \"morphing\" || !s.assembleDone) return; // absorb mid-play\n      if (dir > 0) {\n        if (s.stage === \"A\") {\n          s.setMorphTarget(1, false); // near end → auto-play A→B, STAY engaged\n          armed = false;\n        } else if (s.stage === \"B\") {\n          release(1); // far end → release downward\n        }\n      } else if (s.stage === \"B\") {\n        s.setMorphTarget(0, false); // near end (up) → auto-play B→A, STAY engaged\n        armed = false;\n      } else if (s.stage === \"A\") {\n        release(-1); // far end (up) → release upward\n      }\n    };",
      "change": "    // One discrete gesture → advance ONE leg, or RELEASE at a sequence BOUNDARY.\n    // Release is no longer a letter test: it is \"there is no next node in this\n    // direction\". Interior nodes (Michele at N=3) morph in BOTH directions and\n    // can never release — which is exactly the old \"a near-end gesture can only\n    // morph, never release\" guarantee, generalised.\n    const step = (dir: number) => {\n      const s = useFoundersMorphStore.getState();\n      if (s.stage === \"morphing\" || !s.assembleDone) return; // absorb mid-play\n      const i = s.stageIndex;                    // 0..N-1; never -1 here because\n                                                 // stage !== \"morphing\" was checked\n      const next = i + (dir > 0 ? 1 : -1);\n      if (next >= 0 && next < N) {\n        s.setMorphTarget(next, false);           // interior → auto-play one leg, STAY engaged\n        armed = false;\n      } else {\n        release(dir > 0 ? 1 : -1);               // boundary → hand the page back\n      }\n    };",
      "rationale": "This IS the two-target assumption. The semantic shift the brief asked me to pin down: today \"far end\" is a property of the STAGE (B releases down, A releases up); with three it becomes a property of the (stage, direction) PAIR, because Michele is far-end for neither direction. Encoding it as a bounds check on i+dir is the only formulation correct for any N that also preserves the anti-trap invariant — a gesture at an interior node cannot release, so the user always sees a morph before the page moves. release() itself (896-918) is already purely direction-driven and needs NO change."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "941-970 (consume) and 975-997 (dev harness)",
      "current": "      if (useFoundersMorphStore.getState().stage === \"morphing\") { acc = 0; return; }\n...\n    const getGate = () => { const s = ...; return { engaged, stage: s.stage, morphTarget: s.morphTarget, armed, accum: acc }; };",
      "change": "consume() needs NO change — it is stage-agnostic apart from the `=== \"morphing\"` absorb, which still holds. In getGate() add `stageIndex: s.stageIndex` alongside `stage`. simulateGesture (975-984) is unchanged and is the ONLY reliable way to QA the new sequence.",
      "rationale": "The dev harness currently asserts on letters; with three nodes the index is the unambiguous assertion. QA of this change depends entirely on foundersGateApi — the wheel path is momentum-flaky by design (comment 972-974)."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1241-1253 (teardown copy pose)",
      "current": "      if (copyA) { gsap.set(copyA, { opacity: 1, y: 0 }); copyA.style.visibility = \"\"; }\n      if (copyB) { gsap.set(copyB, { opacity: 0 }); copyB.style.visibility = \"\"; }\n      bChildren.forEach((el) => gsap.set(el, { clearProps: \"opacity,transform\" }));",
      "change": "      blocks.forEach((b, i) => {\n        if (!b) return;\n        gsap.set(b.el, i === 0 ? { opacity: 1, y: 0 } : { opacity: 0, y: 0 });\n        b.el.style.visibility = \"\";\n        b.children.forEach((c) => gsap.set(c.el, { clearProps: \"opacity,transform\" }));\n      });\n(this is why BlockFx.children carries the raw `el` — no second query at teardown)",
      "rationale": "Teardown must settle to the stage-0 rest pose that matches store.reset() (which returns morph to 0). Blocks 1..N-1 must end hidden; a stale `visibility: hidden` or a stale opacity 1 on Mattia would leak into a horizontal-rail / native remount."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1561-1589 (stage posters)",
      "current": "                <div data-founder-stage className=\"relative mx-auto aspect-[3/4] w-full max-w-[26rem]\">\n                  <img ref={stageImgARef} src={founders[0].image} alt={`${founders[0].name}, ...`} data-founder-media draggable={false} style={{ opacity: 0 }} className=\"absolute inset-0 h-full w-full rounded-lg object-cover\" />\n                  <img ref={stageImgBRef} src={founders[1].image} ... />\n                </div>",
      "change": "                <div data-founder-stage className=\"relative mx-auto aspect-[3/4] w-full max-w-[26rem]\">\n                  {founders.map((f, i) => (\n                    // eslint-disable-next-line @next/next/no-img-element\n                    <img\n                      key={f.anchor}\n                      ref={(el) => { stageImgRefs.current[i] = el; }}\n                      src={f.image}\n                      alt={`${f.name}, ${isEn ? f.roleEn : f.roleIt}`}\n                      data-founder-media\n                      draggable={false}\n                      style={{ opacity: 0 }}\n                      className=\"absolute inset-0 h-full w-full rounded-lg object-cover\"\n                    />\n                  ))}\n                </div>\n\nThese use f.image — the DOM poster (/founders/mattia-scattu.webp). The particle sampler's preference for the `-headshot` variant lives in the island; do NOT point the poster at the headshot file.",
      "rationale": "Removes the founders[0]/founders[1] hardcode and keeps alt text in the a11y tree for all three (the stage's only accessible content once the cloud is live)."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1590-1612 (copy blocks)",
      "current": "                <div className=\"relative min-h-[26rem]\">\n                  <div ref={copyARef} className=\"absolute inset-x-0 top-0\">\n                    <FounderCopy f={founders[0]} index={0} total={total} isEn={isEn} />\n                  </div>\n                  <div ref={copyBRef} className=\"absolute inset-x-0 top-0\" style={{ opacity: 0 }}>\n                    <FounderCopy f={founders[1]} index={1} total={total} isEn={isEn} />\n                  </div>\n                </div>",
      "change": "                <div className=\"relative min-h-[26rem]\">\n                  {founders.map((f, i) => (\n                    <div\n                      key={f.anchor}\n                      ref={(el) => { copyRefs.current[i] = el; }}\n                      className=\"absolute inset-x-0 top-0\"\n                      style={i === 0 ? undefined : { opacity: 0 }}\n                    >\n                      <FounderCopy f={f} index={i} total={total} isEn={isEn} />\n                    </div>\n                  ))}\n                </div>\n\nThe inline opacity:0 on i>0 preserves the documented SSR contract at 618-623 (non-first blocks must not flash before the effect arms their children).",
      "rationale": "FounderCopy already takes index/total and renders `01 / 03` correctly once founders.length is 3 (line 209) — the component itself needs no change."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1629-1639 (gate chrome literals)",
      "current": "                  <span data-founders-counter className=\"text-[1.5rem] leading-none tracking-tight text-ink\">\n                    01\n                  </span>\n                  <span className=\"text-[11px] tracking-[0.18em] text-ink-dim\">\n                    / 02\n                  </span>",
      "change": "                  <span data-founders-counter className=\"text-[1.5rem] leading-none tracking-tight text-ink\">\n                    01\n                  </span>\n                  <span className=\"text-[11px] tracking-[0.18em] text-ink-dim\">\n                    / {String(total).padStart(2, \"0\")}\n                  </span>\n\n\"01\" stays literal — it is the SSR seed that applyStage overwrites via textContent, and it is correct for any N.",
      "rationale": "`total` is already `founders.length` (line 1429) and is in scope in this JSX. The denominator is the only hardcode; deriving it means the counter can never drift from the data again."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "1283-1427 + 1665-1700 (HORIZONTAL RAIL), 1514-1534 (NATIVE)",
      "current": "    track.querySelectorAll<HTMLElement>(\"[data-founders-panel]\").forEach((li) => { ... });\n...\n      travel = Math.max(0, track.scrollWidth - vw);\n      section.style.height = `${window.innerHeight + travel}px`;\n...\n          {panels(\"w-[min(88vw,34rem)] shrink-0\")}",
      "change": "NO CHANGE REQUIRED in either fallback. Verified: the rail builds its fx array by QUERYING [data-founders-panel] (1305-1325), derives travel from track.scrollWidth (1329), and panels() maps `founders` (1470-1474) with `total = founders.length` (1429). The native mode is a plain overflow-x scroller over the same panels() (1528). Adding a third entry to src/data/founders.ts lengthens the rail by one panel and grows its runway automatically. FounderPanel's index/total counter (404-407) already renders 03 / 03. Only add a regression check that the rail's extra ~34rem of travel looks right.",
      "rationale": "Explicitly answering the brief: neither fallback hides a hardcoded 2. The two-target assumption is confined to the canMorph branch."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "194-265 (FounderCopy), 267-475 (FounderPanel)",
      "current": "function FounderCopy({ f, index, total, isEn }: { f: FounderProfile; index: number; total: number; isEn: boolean; })\n...\n        {String(index + 1).padStart(2, \"0\")} / {String(total).padStart(2, \"0\")}",
      "change": "NO CHANGE. Both are fully generic over (f, index, total): counters are computed, `previouslyAt` is optional-guarded (236, 455), credentials map over arrays, and the SVG filter/mask ids come from useId() so a third instance stays document-unique. The ONLY coupling is the one already flagged: FounderCopy's child COUNT varies with `previouslyAt`, which is why the enter `dur` must become per-block.",
      "rationale": "Confirms both presentation components generalise; the change surface is the gate, not the components."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "519-525 (canMorph), 572-574 (roomy), 1555 (stage grid)",
      "current": "  const canMorph = detected && mode === \"pinned\" && roomy && tierResolved && tier === \"full\" && backend === \"webgpu\";\n...\n    const roomQ = window.matchMedia(\"(min-width: 1024px) and (min-height: 780px)\");\n...\n              <div className=\"grid grid-cols-1 items-center gap-10 lg:grid-cols-[minmax(0,26rem)_1fr]\">",
      "change": "NO CHANGE to any of the three. The roomy floor (1024x780) sizes the TWO-COLUMN stage — one 26rem portrait beside one copy column — and that layout is identical at N=3 because only ONE founder is on stage at a time. The grid at 1555 is portrait-vs-copy, NOT founder-vs-founder. Do NOT convert it to 3-up; the 3-up card grid change belongs exclusively to /about (about-client.tsx, md:grid-cols-2).",
      "rationale": "The brief's 'md:grid-cols-2 becomes a 3-up layout' targets the About page card grid. Applying it to line 1555 would put three columns inside the sticky morph frame and break the stage."
    }
  ],
  "hazards": [
    "PIN LENGTH IS A TRAP. The pinned window needs ZERO extra px. measure() (783-790) writes section.style.height = innerHeight and setLayout(0, secTop) — travel is literally 0 — and the gate holds the page with lenis.stop() + a per-frame re-snap (tick 1097-1121). Anyone who 'makes room for a third stage' by growing the section height creates dead space the gate immediately re-snaps away from, and moves the [data-line-anchor=\"founders\"] waypoint the header warns about at 46-50. The only thing that must grow is G_MAX_ENGAGE_MS (line 88), 16000 -> ~24000.",
    "childDur (628-633) is computed ONCE from founder B's child list. Mattia has no `previouslyAt`, so his FounderCopy renders 5 children where Michele renders 6. A shared childDur makes his last child's window end at a value != COPY_ENTER_END — i.e. his copy can still be mid-flight when the island locks the stage at m >= 0.98, the exact failure the comment at 100-103 says must never happen. The enter `dur` MUST become per-block.",
    "morphTarget is typed `0 | 1` in the store (81, 106, 123, 143). TypeScript will reject setMorphTarget(2) — a useful compile-time tripwire, but it means the store widening must land in the SAME commit as the gate change or the build breaks.",
    "The island derives stage as `gc <= 0.02 ? \"A\" : gc >= 0.98 ? \"B\" : \"morphing\"` (FounderPortraitMorph.tsx:670) against a [0,1] clamp, and advances uMorph at delta/MORPH_DURATION clamped to [0,1] (659-663). If the gate starts sending morphTarget=2 before the island is widened to [0,N-1], uMorph pins at 1, stage sticks at \"B\", step() sees no progress, and the gate absorbs every gesture until G_MAX_ENGAGE_MS force-releases — a 24-second dead section. The island and the gate MUST ship together.",
    "The reload-landed-inside engage (852, 1137-1151) will engage at index 0 unconditionally — acceptable, but the 600ms window latched on `live.active` (1137-1139) is already fragile per the long comment at 838-851. Do not touch its timing while making this change.",
    "visibility:hidden gating (746-755) is what keeps invisible LinkedIn links out of the tab order. The generalised predicate `exitT >= 1 || u <= COPY_ENTER_START` must hold at both ends: block 0 (u >= 1, never hidden by the enter clause) and block N-1 (exitT always 0, never hidden by the exit clause). Get it wrong and either Alessandro's link vanishes permanently or Mattia's is focusable during Alessandro's stage.",
    "The poster cross-fade is fallback-only (gated on !store.active, 758). It is INVISIBLE on the primary WebGPU target, so a bug there will not appear in normal QA — it only surfaces on a WebGL2 session after the 4s posterGrace (812-817). Verify by forcing posterShown in devtools, not by eyeballing the happy path.",
    "`store.morph` semantics change from 'uMorph 0..1' to 'global leg position 0..N-1'. Any other reader of store.morph outside this file and the island would silently misread it. Grep before landing.",
    "MEMORY.md warns that parallel fix agents previously introduced 7 regressions here including 2 P0s. This spec touches the gate, the store, and the island contract simultaneously — review the combined diff adversarially before declaring done, and QA via foundersGateApi.simulateGesture rather than real wheel events."
  ],
  "openQuestions": [
    "The brief says the eyebrow 'THE FOUNDING PAIR' becomes 'THE TEAM', but this file's eyebrow (1432-1434) is 'Founder-led AI engineering studio' / 'Studio di AI engineering guidato dai fondatori'. 'THE FOUNDING PAIR' lives on /about. Should the HOME eyebrow also change (it says 'Founder-led', and Mattia is not a founder), and to what EN+IT pair? Related: the description at 1458 ('No account layer, no junior bench') and the closer's 'Full founder bios' link (1500) may both want a look now that a non-founder engineer is in the block.",
    "Store shape: I recommend adding `stageIndex: number` (island-derived, -1 while morphing). The alternative — the gate deriving `Math.round(s.morph)` whenever stage !== 'morphing' — needs no store field but duplicates the island's rounding rule across two files. Confirm which the island agent is implementing so we don't ship two sources of truth.",
    "Does Mattia have `previouslyAt`? If yes, his child count matches Michele's and the per-block enter `dur` is defensive rather than load-bearing. I'd make it per-block either way.",
    "G_MAX_ENGAGE_MS at 24s: is a 24-second maximum scroll hijack acceptable? It is a safety valve, not an expected duration, but it is the longest the page can ever be held. The alternative is to reset `engageTime` on every COMPLETED leg (a per-leg timer rather than per-engagement), keeping the number small with the same anti-trap property. I lean toward the per-leg reset, but it changes the safety valve's semantics and should be an explicit decision.",
    "Should the hairline (setChromeLine) track global progress m/(N-1) — one sweep across the whole sequence, reading 0 -> 0.5 -> 1 — or reset per leg (two full sweeps)? I specified global, matching the counter. Per-leg arguably reads as 'more progress'. Taste call."
  ]
}