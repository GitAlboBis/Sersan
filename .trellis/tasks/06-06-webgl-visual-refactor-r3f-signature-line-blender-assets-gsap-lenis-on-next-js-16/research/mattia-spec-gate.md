{
  "subsystem": "state — src/webgl/store/foundersMorphStore.ts + src/webgl/store/founderMotion.ts",
  "summary": "## 1. Every field of the morph state\n\n`FoundersMorphState` (foundersMorphStore.ts:65-116). Only three fields encode \"two\":\n\n- **`stage: FounderStage`** (:79, union at :63 = `\"A\" | \"morphing\" | \"B\"`). NOT authored by the gate — it is DERIVED by the island from its own live uMorph, at FounderPortraitMorph.tsx:670: `const nextStage = gc <= 0.02 ? \"A\" : gc >= 0.98 ? \"B\" : \"morphing\"` and pushed via `setStage` (:671). Writer: island ONLY. Readers: the gate (`step` :925-938, `consume` :957, `scheduleHint` :694, `getGate` :989), the store subscription (:801-804), the dev handle `getStage()` (FounderPortraitMorph.tsx:801).\n- **`morphTarget: 0 | 1`** (:81). The uMorph value the island animates toward. Writers: the gate ONLY — `engage` (:870, immediate), `step` (:928, :934), plus the dev handle `setStage`/`playMorph` (FounderPortraitMorph.tsx:832, :838). Readers: the island's one-shot clock (FounderPortraitMorph.tsx:652, :655) and `getGate` (:990).\n- **`morph: number`** (:84, \"0..1\"). The island's live uMorph reported back each frame (FounderPortraitMorph.tsx:669, guarded by `> 1e-4`). Readers: `applyStage(m)` in the gate (founders-rail.tsx:731-781) via the subscription (:800) and the two priming calls (:794, :815). This field is ALREADY a continuous scalar — that is the crux of my recommendation.\n\nNon-two-encoding fields (unchanged by this work): `pinned` (:67), `active` (:75, written by the island at :547/:594, read reactively by the gate tick :1136 and in `applyStage` :758), `gateEngaged` (:77), `morphImmediate` (:83, consumed+cleared by the island at :651-654 and :649), `assembleDone` (:89, island writer :683, gate reader :925), `reveal` (:91, gate writers :863/:1124, island reader :678/:691), `hover` (:93, gate writers :1180/:1182 — **no reader anywhere**, effectively dead), `mouse` (:95, gate writer :1176, island reader :721), `secTop`/`travel` (:97/:99, `setLayout` writers :788/:1095/:1196, island reader :622-624), `measureVersion` (:101, `bumpMeasure` :789, the island's ONLY reactive read :238 and a build-effect dep :597).\n\n`INITIAL` (:118-132) carries `stage: \"A\"`, `morphTarget: 0 as 0 | 1`, `morph: 0`. `mouse` is deliberately outside INITIAL (fresh object per reset, :137/:155).\n\n**globalThis pin** (:158-166): `globalThis.__sersanFoundersMorph ??= createFoundersMorphStore()`. Note the collision-adjacent naming: Scene.tsx:72-75 ALSO assigns `window.__sersanFoundersMorph = m.useFoundersMorphStore` (the store hook, dev only), while the island's dev handle is `window.__sersanFounderMorph` (singular \"Founder\", FounderPortraitMorph.tsx:757). Two different globals, one letter apart.\n\n**`foundersGateApi`** (:178-186): a plain module singleton (not zustand). `{ simulateGesture(dir), getGate() }`, registered by the gate at founders-rail.tsx:996 (dev only) and cleared at :1264; proxied out to QA by the island at :805-810. `getGate()`'s real return shape is founders-rail.tsx:985-994 = `{ engaged, stage, morphTarget, armed, accum }` — typed `unknown` in the interface, so it needs NO signature change for three stages.\n\n## 2. What a 3-stage generalisation should look like — recommendation\n\n**Recommend (b): the continuous scalar, widened to 0..2 — with `stage` KEPT as a derived 4-value union `\"A\" | \"B\" | \"C\" | \"morphing\"`.**\n\nRationale, against the four criteria:\n\n- **The scalar already exists.** `morph` is a float today and `stage` is derived from it in one line. Design (b) is therefore not a rewrite, it is a range widening (0..1 → 0..2) plus one shared derivation helper. Design (a) does not eliminate the scalar — the island still needs per-leg progress to drive uMorph — so (a) is strictly (b) PLUS a second discrete in-flight token that must be kept mutually consistent with it (`stage === \"morphing2\"` must imply `morph ∈ (1,2)`; nothing enforces that, and a dev `setMorph` override would break it).\n- **Gate state machine.** `step()` today is a 4-branch if-chain over (dir × stage) (founders-rail.tsx:923-939). Under (b) it collapses to arithmetic on a stage INDEX: step up = `i+1` if `i < 2` else release; step down = `i-1` if `i > 0` else release — shorter than the code it replaces, and it stays correct for a 4th person. Under (a) it grows to a 6-branch chain and grows again per person.\n- **Island → uMorph/uMorph2.** (b) gives the mapping for free: `uMorph = min(m,1)`, `uMorph2 = max(0, m-1)`. Critically, the existing clock `cur + dir*delta/MORPH_DURATION` (FounderPortraitMorph.tsx:659-663) stays BYTE-IDENTICAL, because one leg is exactly 1.0 of m — only the clamp ceiling changes from 1 to 2. Under (a) the island would have to select a leg from the union and then run a separate per-leg 0..1 clock, i.e. a second source of truth.\n- **DOM cross-fade + counter.** Every copy window (`COPY_EXIT_*`, `COPY_ENTER_*` :96-109) and the poster `smoothstep(0.35, 0.65, m)` (:757) is authored in LEG space. (b) hands them `fract(m)` and they work unmodified on leg 2 — that is the single biggest saving in the whole change, and it is why the store should export `legOf`/`legFract` rather than letting the gate re-derive them. The counter becomes `floor(m + 0.5) + 1` (flips at each leg midpoint, exactly mirroring today's `m >= 0.5 ? \"02\" : \"01\"`), and the hairline becomes `m / MORPH_MAX`.\n\nWhy NOT a 5-value union with `morphing2`: **no consumer asks which leg is in flight.** All four readers of `\"morphing\"` ask one of two questions — \"is a leg in flight?\" (absorb input :925/:957, hide hint :694/:802) or \"which locked end am I on?\" (step direction, release). Splitting `morphing` would force every one of those sites to become a two-value test and buy nothing.\n\n## 3. founderMotion.ts — live vs orphaned\n\nRepo-wide grep for all seven exports returns only `founderMotion.ts` itself, `founders-rail.tsx`, and prose in `WEBGL_UPGRADE_PLAN.md`.\n\n- **LIVE:** `founderCardMotion` — imported at founders-rail.tsx:24, called at :1347 inside the HORIZONTAL RAIL effect (the non-morph desktop fallback). `FOUNDER_ARC_PX` (:34) and `FOUNDER_FOCUS_SCALE` (:36) are live transitively (used at :63-64), as is the `FounderCardMotion` interface.\n- **ORPHANED:** `FOUNDER_TILT_MAX` (:43) and `FOUNDER_LENS_RADIUS` (:49) — their sole code consumer was the deleted `FounderPlanes.tsx`. Only the plan .md still mentions them.\n- **Does anything there assume 2? No.** `founderCardMotion(centerX, vw)` is a pure function of ONE card's viewport-center X; it has no notion of card count or of A/B. The rail iterates `[data-founders-panel]` elements produced by `founders.map` (:1470-1471), and `total = founders.length` (:1429), so a third founder flows through the fallback rail with zero code change. The only defect is doc rot: the header (:11-13) still claims `webgl/FounderPlanes.tsx` mirrors these offsets — that file is deleted.\n\n**No functional change is required in founderMotion.ts for this feature.** Only the two dead constants and the stale header reference (optional cleanup, listed below and safe to defer).",
  "changes": [
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "63",
      "current": "export type FounderStage = \"A\" | \"morphing\" | \"B\";",
      "change": "Widen the union to name all three locked ends, keep ONE in-flight value, and add the shared leg algebra that the gate, the island and the chrome must all share instead of re-deriving thresholds:\n\nexport type FounderStage = \"A\" | \"B\" | \"C\" | \"morphing\";\n\n/** Number of morph LEGS on the rail (people − 1). `morph` spans 0..MORPH_MAX. */\nexport const MORPH_MAX = 2;\n/** People on the rail — the gate counter's denominator (01/03 …). */\nexport const STAGE_TOTAL = MORPH_MAX + 1;\n/** Lock tolerance: within this of an integer the stage counts as LOCKED. */\nexport const LOCK_EPS = 0.02;\n/** Locked stages in rail order; index == the integer value of `morph`. */\nexport const STAGE_ORDER = [\"A\", \"B\", \"C\"] as const;\n/** Locked-stage index 0..MORPH_MAX, or -1 while a leg is in flight. */\nexport const stageIndex = (s: FounderStage): number =>\n  s === \"morphing\" ? -1 : STAGE_ORDER.indexOf(s as (typeof STAGE_ORDER)[number]);\n/** The SINGLE derivation of stage from the live scalar (island writes it). */\nexport const stageFromMorph = (m: number): FounderStage => {\n  for (let i = 0; i <= MORPH_MAX; i++) {\n    if (Math.abs(m - i) <= LOCK_EPS) return STAGE_ORDER[i];\n  }\n  return \"morphing\";\n};\n/** Current leg 0..MORPH_MAX-1 (which pair of portraits is in play). */\nexport const legOf = (m: number): number =>\n  Math.min(Math.max(Math.floor(m), 0), MORPH_MAX - 1);\n/** Progress 0..1 WITHIN the current leg — the value every copy/poster window\n * (COPY_EXIT_*, COPY_ENTER_*, the poster smoothstep) was authored against. */\nexport const legFract = (m: number): number => m - legOf(m);",
      "rationale": "This is the whole design in one place. Exporting stageFromMorph kills the duplicated 0.02/0.98 thresholds currently inlined at FounderPortraitMorph.tsx:670; exporting legFract is what lets every existing 0..1-authored DOM window survive leg 2 unmodified. STAGE_ORDER/stageIndex make step() arithmetic instead of a branch chain, so a 4th person needs no state edit at all."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "79-84",
      "current": "  /** Logical stage — derived by the island from its live uMorph. */\n  stage: FounderStage;\n  /** uMorph the island animates toward (0 = Alessandro, 1 = Michele). */\n  morphTarget: 0 | 1;\n  /** When true the island JUMPS uMorph to morphTarget (no auto-play). */\n  morphImmediate: boolean;\n  /** Live uMorph 0..1 (writer: island; reader: DOM copy/poster cross-fade). */\n  morph: number;",
      "change": "  /** Logical stage — derived by the island via stageFromMorph(morph). */\n  stage: FounderStage;\n  /** Rail position the island animates toward: 0 = Alessandro, 1 = Michele,\n   * 2 = Mattia. Integer by construction — the gate only ever steps by one. */\n  morphTarget: 0 | 1 | 2;\n  /** When true the island JUMPS morph to morphTarget (no auto-play). */\n  morphImmediate: boolean;\n  /** Live rail scalar 0..MORPH_MAX (writer: island; readers: the DOM copy /\n   * poster cross-fade and the gate chrome). floor() = leg, fract() = leg\n   * progress — use legOf()/legFract(), never raw arithmetic. */\n  morph: number;",
      "rationale": "morphTarget is the only field whose TYPE encodes two targets; widening it to 0|1|2 is the minimal type change. The morph doc must state the new range explicitly or a future reader will keep authoring 0..1 windows against the raw value — the single most likely regression in this change."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "106",
      "current": "  setMorphTarget: (morphTarget: 0 | 1, immediate?: boolean) => void;",
      "change": "  setMorphTarget: (morphTarget: 0 | 1 | 2, immediate?: boolean) => void;",
      "rationale": "Action signature must accept the third target. The implementation body (:143-144) needs NO change — it is already a plain set of both fields."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "122-123",
      "current": "  stage: \"A\" as FounderStage,\n  morphTarget: 0 as 0 | 1,",
      "change": "  stage: \"A\" as FounderStage,\n  morphTarget: 0 as 0 | 1 | 2,",
      "rationale": "INITIAL's cast must widen with the field type or TS narrows the store's inferred type back to 0|1 and every setMorphTarget(2) call fails to typecheck. `morph: 0` (:125) and `stage: \"A\"` are already correct for a 3-stage rail — the entry always assembles Alessandro first."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "14-23, 30-31",
      "current": "(header) \" *   - stage 'A'  : Alessandro fully formed and LOCKED (uMorph pinned at 0).\\n *   - one scroll-down gesture → morphTarget = 1 → the island AUTO-PLAYS A→B …\\n *   - stage 'B'  : Michele fully formed and LOCKED (uMorph pinned at 1).\\n *   - another scroll-down gesture → the gate RELEASES (Lenis resumes).\"  and  \" *   - stage        : 'A' | 'morphing' | 'B' — logical stage (island-derived).\\n *   - morphTarget  : 0 | 1 — the uMorph value the island animates toward.\"",
      "change": "Rewrite the two blocks for the three-stage rail: stage 'A' (Alessandro, morph 0) → gesture → auto-play leg 0 → stage 'B' (Michele, morph 1) → gesture → auto-play leg 1 → stage 'C' (Mattia, morph 2) → gesture → RELEASE; reverse symmetrically upward, and state explicitly that B is now a MIDDLE stage from which NEITHER direction releases (Escape and the G_MAX_ENGAGE_MS timer remain the only outs from B). FIELDS block: \"stage: 'A' | 'B' | 'C' | 'morphing' — island-derived via stageFromMorph\" and \"morphTarget: 0 | 1 | 2 — the rail position the island animates toward\".",
      "rationale": "This header is the spec the gate and island are both written against; leaving it describing a two-target rail is how the next agent reintroduces the 0..1 assumption. The B-is-non-releasing consequence is a real behaviour change that must be written down, not discovered."
    },
    {
      "file": "src/webgl/store/foundersMorphStore.ts",
      "lines": "180-182",
      "current": "  /** Inject exactly ONE discrete armed gesture into the gate state machine. */\n  simulateGesture: (dir: \"up\" | \"down\") => unknown;\n  /** { engaged, stage, morphTarget, armed, accum } snapshot. */\n  getGate: () => unknown;",
      "change": "No signature change. Update the getGate doc only: \"{ engaged, stage: 'A'|'B'|'C'|'morphing', morphTarget: 0|1|2, armed, accum } snapshot.\" and extend the block comment above (:168-177) so QA's documented sequence reads A→B→C→release / C→B→A→release rather than A→B→release.",
      "rationale": "Both members are typed `unknown`/opaque, so the three-stage values flow through with zero code change — this is a doc-only edit, and it is the reason design (b) touches the dev API not at all. Verified: the concrete shape is produced at founders-rail.tsx:985-994 and consumed only by FounderPortraitMorph.tsx:805-810."
    },
    {
      "file": "src/webgl/store/founderMotion.ts",
      "lines": "11-13, 43, 49",
      "current": "(header) \" *   - webgl/FounderPlanes.tsx mirrors the SAME scale/y offsets in its camera-\\n *     locked billboard placement and feeds `f` into the plane material's uFocus.\"  plus  \"export const FOUNDER_TILT_MAX = 0.12;\"  and  \"export const FOUNDER_LENS_RADIUS = 0.42;\"",
      "change": "Optional cleanup, NOT required by this feature: delete the FounderPlanes bullet from the header (that file is deleted, so the \"one formula, two consumers\" contract now has exactly one consumer — founders-rail.tsx:1347), and delete FOUNDER_TILT_MAX (:38-43 incl. its comment) and FOUNDER_LENS_RADIUS (:45-49 incl. its comment), which have no code references anywhere in the repo.",
      "rationale": "Confirms the answer to the brief's Q4: nothing in founderMotion.ts assumes two founders — founderCardMotion is a pure per-card function with no count dependency, and the fallback rail already iterates founders.length. Only two dead exports and one stale doc reference. Safe to defer; do NOT let it block the state change."
    },
    {
      "file": "src/webgl/FounderPortraitMorph.tsx",
      "lines": "648, 659-663, 666-671",
      "current": "      morphRef.current = THREE.MathUtils.clamp(override, 0, 1);\n…\n        morphRef.current = THREE.MathUtils.clamp(\n          cur + (dir * delta) / MORPH_DURATION,\n          0,\n          1,\n        );\n…\n    const gc = THREE.MathUtils.clamp(morphRef.current, 0, 1);\n    b.uMorph.value = gc;\n    if (Math.abs(store.morph - gc) > 1e-4) store.setMorph(gc);\n    const nextStage = gc <= 0.02 ? \"A\" : gc >= 0.98 ? \"B\" : \"morphing\";\n    if (store.stage !== nextStage) store.setStage(nextStage);",
      "change": "CONSUMER SEAM (island subsystem owns the edit; this is the contract it must honour). Replace all three clamp ceilings 1 → MORPH_MAX, split the scalar across the two uniforms, and delegate the stage derivation to the store:\n\n    const gc = THREE.MathUtils.clamp(morphRef.current, 0, MORPH_MAX);\n    b.uMorph.value = Math.min(gc, 1);\n    b.uMorph2.value = Math.max(0, gc - 1);\n    if (Math.abs(store.morph - gc) > 1e-4) store.setMorph(gc);\n    const nextStage = stageFromMorph(gc);\n    if (store.stage !== nextStage) store.setStage(nextStage);\n\nThe flight envelope at :674 must also become per-leg: `const env = Math.sin(legFract(gc) * Math.PI)` — otherwise sin(gc·π) returns to 0 at m=1 and NEGATIVE beyond it, killing the spread/orbit on leg 2 and inverting the yaw. Import { MORPH_MAX, stageFromMorph, legFract } from ./store/foundersMorphStore.",
      "rationale": "The clock formula at :659-663 needs NO other change — one leg is exactly 1.0 of the scalar, so the existing delta/MORPH_DURATION rate already yields the same per-leg duration. This is design (b)'s payoff and the reason I recommend it. The sin(gc·π) envelope is the one non-obvious 0..1 assumption hidden in the island; it produces a negative multiplier on leg 2 if missed."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "923-939",
      "current": "    const step = (dir: number) => {\n      const s = useFoundersMorphStore.getState();\n      if (s.stage === \"morphing\" || !s.assembleDone) return; // absorb mid-play\n      if (dir > 0) {\n        if (s.stage === \"A\") {\n          s.setMorphTarget(1, false); // near end → auto-play A→B, STAY engaged\n          armed = false;\n        } else if (s.stage === \"B\") {\n          release(1); // far end → release downward\n        }\n      } else if (s.stage === \"B\") {\n        s.setMorphTarget(0, false); // near end (up) → auto-play B→A, STAY engaged\n      \tarmed = false;\n      } else if (s.stage === \"A\") {\n        release(-1); // far end (up) → release upward\n      }\n    };",
      "change": "CONSUMER SEAM (gate subsystem owns the edit). Replace the branch chain with index arithmetic — release only from the two EXTREME ends, step otherwise:\n\n    const step = (dir: number) => {\n      const s = useFoundersMorphStore.getState();\n      if (s.stage === \"morphing\" || !s.assembleDone) return; // absorb mid-play\n      const i = stageIndex(s.stage); // 0 | 1 | 2 (never -1 past the guard)\n      const next = i + (dir > 0 ? 1 : -1);\n      if (next < 0) return release(-1);      // far end (up) → release upward\n      if (next > MORPH_MAX) return release(1); // far end (down) → release down\n      s.setMorphTarget(next as 0 | 1 | 2, false);\n      armed = false;\n    };\n\nImport { MORPH_MAX, stageIndex } alongside the existing store imports at :20-22.",
      "rationale": "Preserves the existing invariant verbatim ('a near-end gesture can only ever morph, never release') while generalising it, and is shorter than the code it replaces. Under design (a) this same function becomes a 6-branch switch. Note the behaviour consequence to sign off: stage B is now a middle stage, so neither direction releases from it — the anti-trap surface reduces to Escape (:1016) and G_MAX_ENGAGE_MS (:1121)."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "870, 1148",
      "current": "      s.setMorphTarget(initStage === \"B\" ? 1 : 0, true);\n…\n            engage(fromBottom ? \"B\" : \"A\");",
      "change": "CONSUMER SEAM. The entry from BELOW must land on the LAST person, not the second. Retype engage's param and map through MORPH_MAX:\n\n    const engage = (initStage: \"start\" | \"end\") => { …\n      s.setMorphTarget(initStage === \"end\" ? (MORPH_MAX as 0 | 1 | 2) : 0, true);\n…\n            engage(fromBottom ? \"end\" : \"start\");\n\nThe reload-landed-inside call at :1150, `engage(\"A\")`, becomes `engage(\"start\")`.",
      "rationale": "engage(\"B\") is a literal two-target encoding: arriving upward from below must pin Mattia (morph 2), or the user scrolling up into the section is dropped mid-rail at Michele and the very first up-gesture then plays a leg BACKWARD from a stage they never saw. Renaming the param start/end also stops it being restated per person."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "767-780",
      "current": "      if (setChromeLine) {\n        const q = Math.round(m * 512) / 512;\n        if (q !== lastLineQ) {\n          lastLineQ = q;\n          setChromeLine(q);\n        }\n      }\n      if (chromeCounterEl) {\n        const label = m >= 0.5 ? \"02\" : \"01\";\n        if (label !== lastCounter) {\n          lastCounter = label;\n          chromeCounterEl.textContent = label;\n        }\n      }",
      "change": "CONSUMER SEAM. Normalise the hairline to the full rail and derive the counter from the nearest stage index:\n\n      if (setChromeLine) {\n        const q = Math.round((m / MORPH_MAX) * 512) / 512;\n        if (q !== lastLineQ) { lastLineQ = q; setChromeLine(q); }\n      }\n      if (chromeCounterEl) {\n        const idx = Math.min(Math.floor(m + 0.5), MORPH_MAX);\n        const label = String(idx + 1).padStart(2, \"0\");\n        if (label !== lastCounter) { lastCounter = label; chromeCounterEl.textContent = label; }\n      }\n\nThe denominator is hardcoded in the JSX at :1637 (`/ 02`) — change to `/ {String(founders.length).padStart(2, \"0\")}` so it tracks the data, and update the stale comments at :1616 (\"01/02 → 02/02\") and :646.",
      "rationale": "scaleX must stay 0..1 or the hairline overshoots its 16rem track by 2× on leg 2. The floor(m+0.5) form reproduces today's midpoint flip exactly (m=0.5 → \"02\", m=1.5 → \"03\") so the counter keeps its established timing, and the min() guards m=2 from reading \"04\"."
    },
    {
      "file": "src/components/sections/founders-rail.tsx",
      "lines": "731-764, 794, 800, 815",
      "current": "    const applyStage = (m: number) => {\n      const exitT = smoothstep(COPY_EXIT_START, COPY_EXIT_END, m);\n…\n      const bF = smoothstep(0.35, 0.65, m);\n…\n    applyStage(store.morph);\n…\n      if (s.morph !== prev.morph || s.active !== prev.active) applyStage(s.morph);",
      "change": "CONSUMER SEAM (DOM subsystem owns the three-block cross-fade itself). The state contract: applyStage must receive the LEG and the LEG-LOCAL progress, not the raw scalar — `applyStage(legOf(m), legFract(m))` at all three call sites (:794, :800, :815) — and every existing window (COPY_EXIT_START/END, the bFx child windows, the poster smoothstep(0.35,0.65,·)) then reads the 0..1 leg progress UNCHANGED. Within a leg, the outgoing block is founders[leg] and the incoming is founders[leg+1]; the chrome branch above still needs the raw m, so keep passing it or recompute as `leg + f`.",
      "rationale": "Every copy/poster constant at :96-109 and :757 is authored in 0..1 leg space. Feeding them the raw 0..2 scalar silently saturates every smoothstep for all of leg 2 — founder A's copy stays fully exited, B's stays fully entered, Mattia never appears, and nothing throws. This is the highest-risk silent failure in the whole change, and the reason legOf/legFract belong in the store rather than being open-coded in the gate."
    }
  ],
  "hazards": [
    "ENGINE BLOCKER — colour and ink are uMorph-ONLY, two sets. gpgpuNodeSim.ts:1167-1172 builds portraitMorphExpr from `morphN` alone, and PortraitMorphOpts (:841-851) accepts exactly colorsA/colorsB and sizeA/sizeB. So driving uMorph2 morphs POSITIONS to homeC while Mattia's particles keep MICHELE's per-particle colours and ink. The state change alone therefore produces a Michele-coloured Mattia. Two exits: (1) extend the engine with colorsC/sizeC and blend on a second staggered expression from morph2N; or (2) leave the engine alone and REBUILD the cloud per leg with the (current, next) portrait pair — which conflicts with the shared-grid pairing in samplePortraitPair (one shared cell list pairs exactly two images). Whoever owns the engine subsystem must decide; the state design is neutral between them.",
    "globalThis pin survives HMR across a SHAPE change. `globalThis.__sersanFoundersMorph ??= createFoundersMorphStore()` (foundersMorphStore.ts:165-166) never re-creates once set, so after editing the store in a running dev session the island keeps reading the OLD two-target instance — setMorphTarget(2) lands on a store whose consumers were never rebuilt. Symptom is a rail that refuses to leave B with no error. Hard-reload the tab after this edit before believing any QA result.",
    "The flight envelope `Math.sin(gc * Math.PI)` (FounderPortraitMorph.tsx:674) is a hidden 0..1 assumption: at m=1 it is exactly 0 (harmless) but for m ∈ (1,2] it goes NEGATIVE, so uSpread, the dolly (:701) and the orbit yaw (:725) all invert on leg 2 — the cloud dollies AWAY and orbits backwards. Must become legFract-based.",
    "The rebuild seed picks a target by a 0.5 threshold: FounderPortraitMorph.tsx:424, `seed = (morphRef.current >= 0.5 ? homeB : homeA).slice()`. On a resize/measure rebuild while parked at Mattia this seeds Michele's cloud, i.e. a visible identity snap. Needs a three-way pick keyed on the rounded stage index.",
    "Dev handle drift: FounderPortraitMorph.tsx:830-839 — `setStage(s: \"A\" | \"B\")` maps to setMorphTarget(s === \"B\" ? 1 : 0) and `playMorph(dir)` sets an absolute 1 or 0. Both are two-target literals; after the widening they can drive the rail to an unreachable state (playMorph(1) from stage C is a no-op, playMorph(-1) from C jumps two legs at once through the one-shot clock). Also `morphOverrideRef` is clamped to (0,1) at :648 so QA can never inspect leg 2 mid-flight.",
    "B becomes a MIDDLE stage with no release in either direction. Today every locked stage is an extreme end, so the user is always one gesture from escaping. After the change, a user parked at Michele must traverse a full leg (1.4s of forced auto-play) to reach an end before the gate hands the page back. G_MAX_ENGAGE_MS is 16000 (founders-rail.tsx:88) and now has to cover up to four gestures plus two 1.4s legs plus the G_IDLE_MS re-arm between each — a slow reader can hit the safety release mid-rail.",
    "`hover` (foundersMorphStore.ts:93) is written by the gate at :1180/:1182 but read by NOBODY — the island reads `mouse` only (FounderPortraitMorph.tsx:721). It is dead state today; do not add a third-stage behaviour on top of it assuming it is wired.",
    "Naming trap for QA: `window.__sersanFoundersMorph` (Scene.tsx:72-75) is the STORE HOOK, while `window.__sersanFounderMorph` (singular Founder, FounderPortraitMorph.tsx:757) is the tuning/gate handle. One letter apart, and only the second one has simulateGesture."
  ],
  "openQuestions": [
    "Does leg 2 get Mattia's real colours and ink, or is a position-only morph acceptable for v1? This is a gpgpuNodeSim change (colorsC/sizeC + a morph2-driven blend expression), not a state change, and it determines whether the state work can ship independently. It is the only true blocker I found.",
    "Arriving from BELOW: engage(\"end\") pins Mattia (morph 2), which I recommend for symmetry. Confirm the boss wants the upward entry to land on the newest team member rather than on Michele — it means a user scrolling up the page meets Mattia first.",
    "Should G_MAX_ENGAGE_MS (16000, founders-rail.tsx:88) rise to ~24000 now that the full rail needs four gestures and two 1.4s legs instead of two gestures and one leg?",
    "Should a mid-rail escape exist at all (e.g. Escape already works, but should a hard down-flick at B release rather than step)? Current rule 'release only from the far end' means B traps the user into completing a leg. I preserved the existing rule; a deliberate exception is a product decision.",
    "MORPH_DURATION is 1.4s per leg (FounderPortraitMorph.tsx:162). Two legs plus the hint's HINT_IDLE_MS 1200ms dwell at each of three stages makes the section noticeably longer to traverse — is the per-leg duration still right at three people, or should it drop to ~1.1s?",
    "Where should MORPH_MAX/STAGE_TOTAL ultimately come from — the literal 2 I propose in the store, or derived from `founders.length` in src/data/founders.ts? Deriving is DRY-er but couples the WebGL store to page data and makes the 0|1|2 literal union type impossible (it would have to become plain `number`), losing the compile-time guarantee that morphTarget is a valid stage."
  ]
}