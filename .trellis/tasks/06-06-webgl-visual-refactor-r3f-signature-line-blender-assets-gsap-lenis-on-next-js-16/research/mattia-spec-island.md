{
  "subsystem": "Data model (`src/data/founders.ts`) + all non-morph surfaces that consume it: /about page, who-and-why (orphan), /contact, /start, about metadata, and the founders.* i18n keys.",
  "summary": "The two-target assumption is encoded in FOUR distinct places in this subsystem, only one of which is a real array-length assumption.\n\n1. `src/data/founders.ts:39-111` — `founders: FounderProfile[]` has exactly two entries. It is the single source of truth and is imported by SIX consumers: `about-client.tsx:18`, `who-and-why.tsx:5`, `contact-client.tsx:7`, `start/page.tsx:4`, `founders-rail.tsx:16`, `FounderPortraitMorph.tsx:73`. There is NO `kind` / role discriminator, so every consumer that does `founders.map(...)` picks up a third entry automatically and unconditionally. That is the core hazard: the user wants Mattia on home + /about ONLY, but `/contact` (`contact-client.tsx:181`) and `/start` (`start/page.tsx:106`) both do a bare `founders.map(...)` and would silently gain a third row labelled \"Talk to a founder\" / \"Who reads this\".\n\n2. `about-client.tsx:203-206` — the grid is `className=\"grid md:grid-cols-2 gap-8 max-w-5xl mx-auto\"`. Hard 2-up. This is the only literal `2` in a layout in this subsystem for the About founders block. (`who-and-why.tsx:122` has the same `md:grid-cols-2`, but that file is orphaned — see below.)\n\n3. Prose that literally asserts \"two\" — NOT array-driven, so it silently becomes false:\n   - `about-client.tsx:143` eyebrow `\"The founding pair\"` / `\"La coppia fondatrice\"`  ← the eyebrow the brief says must become \"THE TEAM\"\n   - `about-client.tsx:150-163` H1 `Two operators. / One thesis.` (IT `Due operatori. / Una sola tesi.`)\n   - `about-client.tsx:168-169` sub `\"...Both senior. Both staffed on every engagement...\"`\n   - `about-client.tsx:197-199` `\"Two opposite skill sets, one thesis.\"` / `\"Due competenze opposte, un'unica tesi.\"`\n   - `about-client.tsx:275-278` closer `\"The founding thesis: deep engineering and deep commercial in the same room...\"`\n   - `about/page.tsx:7` metadata description `\"Sersan was founded by two operators with opposite backgrounds...\"`\n   - `who-and-why.tsx:87-88` `\"...by two founders with complementary depth.\"` (IT `\"da due fondatori...\"`)\n   - `who-and-why.tsx:294-298` `\"Sersan was built by two people with opposite backgrounds.\"` and `:310-315` the IT twin\n   - `start/page.tsx:102-104` source comment `\"the brief literally lands with one of these two people\"`\n\n4. The counter-parallax ScrollTrigger, `about-client.tsx:38-70`. VERDICT: it does NOT assume two. `portraitDriftRefs` is `useRef<(HTMLDivElement|null)[]>([])` written by index at `:222-224`; the effect filters non-null (`:41-43`), builds one quickSetter per target (`:46-48`), early-returns when empty (`:44`), and the onUpdate loops `for (const s of setters)` (`:62`) writing the SAME `y` to all of them. It is index-count-agnostic and needs zero change for a third card. The only caveat is that the effect has `[]` deps, so it must still run after the third ref is populated — it does, because all three cards mount in the same commit as the effect.\n\n5. Duplicated portrait CSS: `about-client.tsx:451-481` (inline `<style>`) vs `founders-rail.tsx:133-169` (`PORTRAIT_CSS` const). They are near-identical and both carry a \"keep them in sync\" comment (`about-client.tsx:445-450`, `founders-rail.tsx:121-132`). Diffs are real, not accidental: founders-rail's `.founder-portrait` transition also animates `border-color 0.3s ease` (`:141`) because there the class sits on the card ROOT, and founders-rail adds a `[data-founder-stage] [data-founder-media] { transition: opacity 0.45s ease; }` rule (`:166-168`) that About has no use for. Everything else (`@property --fr-hr`, `__base` grayscale(1) brightness(0.85), `__color` clip circle, `__ring` #3BE1FF +1.5px annulus, hover 150%, reduced-motion) is byte-identical. Adding a third card does NOT require touching this CSS — it is class-based and card-count-independent. Extracting it is a tempting but OUT-OF-SCOPE refactor.\n\n6. Stale i18n: `founders.sebastiano.role/bio` and `founders.andrea.role/bio` (`en.ts:89-92`, `it.ts:89-92`) name people who no longer exist at Sersan; the values were rewritten to describe Alessandro and Michele but the KEYS still say sebastiano/andrea. `FounderProfile.roleKey`/`bioKey` (`founders.ts:13-14`, populated at `:47-48` and `:82-83`) point at them. CONFIRMED DEAD: a repo-wide grep for `roleKey|bioKey|founders\\.(title|subtitle|mission|sebastiano|andrea)` returns ONLY the definitions in `founders.ts` and the string literals in `en.ts`/`it.ts` — plus one mention in a trellis research note (`.trellis/tasks/.../2026-07-07-cards-scroll-refactor-plan.md:133` which itself calls them \"dead roleKey/bioKey\"). No `t('founders.…')` call site exists anywhere. `founders.title/subtitle/mission.*` are equally unreferenced. RECOMMENDATION: leave them alone. They are out of scope, and deleting `roleKey`/`bioKey` from the interface is a separate (safe but unrelated) cleanup that would force churn in all three entries. If you touch them at all, do it in its own commit.\n\n7. `who-and-why.tsx` is CONFIRMED ORPHANED. Grep for `who-and-why|WhoAndWhy` across `src/` returns matches ONLY inside the file itself (its own JSDoc `:10`, the `export default function` `:13`, and the `aria-labelledby`/`id` pair `:41`/`:58`). Nothing imports it. RECOMMENDATION: do NOT touch it in this change. It renders nothing, its `md:grid-cols-2` (`:122`) and its \"two founders\" prose (`:87-88`, `:295-297`) reach no user, and editing it adds diff noise plus a false impression that the string was fixed on a live surface. Flag it to the user as \"dead file, still says 'two founders', delete or revive separately\".",
  "changes": [
    {
      "file": "src/data/founders.ts",
      "lines": "1-4",
      "current": "// Founder images live in /public/founders/ — referenced as absolute paths\n// rather than imported, so Next.js handles them as static assets.\nconst alessandroImg = \"/founders/alessandro-serratt.webp\";\nconst micheleImg = \"/founders/michele-sanna.webp\";",
      "change": "// Founder images live in /public/founders/ — referenced as absolute paths\n// rather than imported, so Next.js handles them as static assets.\nconst alessandroImg = \"/founders/alessandro-serratt.webp\";\nconst micheleImg = \"/founders/michele-sanna.webp\";\nconst mattiaImg = \"/founders/mattia-scattu.webp\";",
      "rationale": "Same const-per-person convention as the existing two. The DOM poster is mattia-scattu.webp (1200x1800, already on disk); the -headshot variant is consumed only by the WebGL sampler, which resolves it by slug, not from this field."
    },
    {
      "file": "src/data/founders.ts",
      "lines": "6-14",
      "current": "export interface FounderProfile {\n  name: string;\n  initials: string;\n  image: string;\n  anchor: string;\n  linkedIn: string;\n  badges: string[];\n  roleKey: string;\n  bioKey: string;",
      "change": "export interface FounderProfile {\n  name: string;\n  initials: string;\n  image: string;\n  anchor: string;\n  linkedIn: string;\n  badges: string[];\n  /** Founder vs employed team member. Surfaces that say the word \"founder\"\n   * (contact's \"Talk to a founder\", /start's \"Who reads this\") MUST filter on\n   * kind === \"founder\"; the home rail and /about render everyone. */\n  kind: \"founder\" | \"team\";\n  roleKey: string;\n  bioKey: string;\n\nDeclare it REQUIRED (`kind:`), not optional (`kind?:`).",
      "rationale": "THE central mechanism. Without a discriminator, every `founders.map(...)` in the codebase gains Mattia. Making it REQUIRED forces the compiler to make you annotate the two existing entries, so no consumer can be surprised later. Placed next to `badges` so it reads as identity metadata, not presentation."
    },
    {
      "file": "src/data/founders.ts",
      "lines": "41-48",
      "current": "    name: \"Alessandro Serratt\",\n    initials: \"AS\",\n    image: alessandroImg,\n    anchor: \"alessandro\",\n    linkedIn: \"https://www.linkedin.com/in/alessandro-serratt/\",\n    badges: [\"USAAI CAIC\", \"Dual MBA + AI\"],\n    roleKey: \"founders.sebastiano.role\",\n    bioKey: \"founders.sebastiano.bio\",",
      "change": "    name: \"Alessandro Serratt\",\n    initials: \"AS\",\n    image: alessandroImg,\n    anchor: \"alessandro\",\n    linkedIn: \"https://www.linkedin.com/in/alessandro-serratt/\",\n    badges: [\"USAAI CAIC\", \"Dual MBA + AI\"],\n    kind: \"founder\",\n    roleKey: \"founders.sebastiano.role\",\n    bioKey: \"founders.sebastiano.bio\",",
      "rationale": "Annotate the existing entry. Leave the stale sebastiano/andrea keys untouched — dead, out of scope (see summary §6)."
    },
    {
      "file": "src/data/founders.ts",
      "lines": "76-83",
      "current": "    name: \"Michele Sanna\",\n    initials: \"MS\",\n    image: micheleImg,\n    anchor: \"michele\",\n    linkedIn: \"https://www.linkedin.com/in/michele-sanna-work/\",\n    badges: [\"PhD, LSE\", \"8 yrs senior delivery\"],\n    roleKey: \"founders.andrea.role\",\n    bioKey: \"founders.andrea.bio\",",
      "change": "    name: \"Michele Sanna\",\n    initials: \"MS\",\n    image: micheleImg,\n    anchor: \"michele\",\n    linkedIn: \"https://www.linkedin.com/in/michele-sanna-work/\",\n    badges: [\"PhD, LSE\", \"8 yrs senior delivery\"],\n    kind: \"founder\",\n    roleKey: \"founders.andrea.role\",\n    bioKey: \"founders.andrea.bio\",",
      "rationale": "Annotate the existing entry."
    },
    {
      "file": "src/data/founders.ts",
      "lines": "110-111 (insert a third object literal after Michele's closing `},` and before the array's closing `];`)",
      "current": "    authorRole: \"Co-Founder & CPTO, SERSAN\",\n    authorBio:\n      \"Enterprise architect and ML engineer. Builds AI-powered software, data platforms, and the production systems that run them.\",\n  },\n];",
      "change": "    authorRole: \"Co-Founder & CPTO, SERSAN\",\n    authorBio:\n      \"Enterprise architect and ML engineer. Builds AI-powered software, data platforms, and the production systems that run them.\",\n  },\n  {\n    name: \"Mattia Scattu\",\n    initials: \"MSc\",\n    image: mattiaImg,\n    anchor: \"mattia\",\n    linkedIn: \"https://www.linkedin.com/in/mattia-scattu-481271356\",\n    badges: [\"BSc Computer Science\", \"Published research\"],\n    kind: \"team\",\n    roleKey: \"\",\n    bioKey: \"\",\n    roleEn: \"Software Engineer\",\n    roleIt: \"Software Engineer\",\n    accent: \"cool\",\n    credentialsEn: [\n      \"BSc Computer Science, Università di Camerino\",\n      \"Published: Knowledge Graphs as a Semantic Layer for Understanding Robotic Video\",\n    ],\n    credentialsIt: [\n      \"Laurea Triennale in Informatica, Università di Camerino\",\n      \"Pubblicazione: Knowledge Graphs as a Semantic Layer for Understanding Robotic Video\",\n    ],\n    shortBioEn:\n      \"Designs and builds internal systems end to end — requirements, data model, interface, delivery. Shipped a maintenance and inventory management system for a resort operator, from formal requirements analysis through to the running software.\",\n    shortBioIt:\n      \"Progetta e realizza sistemi gestionali interni dall'inizio alla fine: requisiti, modello dati, interfaccia, rilascio. Ha realizzato un sistema di gestione della manutenzione e dell'inventario per un operatore turistico, dall'analisi formale dei requisiti fino al software in esercizio.\",\n    bioEn:\n      \"Software engineer with a Computer Science degree from Università di Camerino. At L'Ultima Spiaggia S.r.l. he designed and built the information system for a campsite resort, covering maintenance operations and inventory tracking: formal requirements analysis, logical and physical database modelling, interface design, and implementation across the full software lifecycle. Previously an IT intern at ARES Sardegna. Co-author of \\\"Knowledge Graphs as a Semantic Layer for Understanding Robotic Video\\\".\",\n    bioIt:\n      \"Software engineer, laureato in Informatica all'Università di Camerino. In L'Ultima Spiaggia S.r.l. ha progettato e realizzato il sistema informativo di un villaggio turistico, per la gestione della manutenzione e il tracciamento dell'inventario: analisi formale dei requisiti, modellazione logica e fisica della base dati, progettazione dell'interfaccia e implementazione lungo l'intero ciclo di vita del software. In precedenza stagista IT presso ARES Sardegna. Co-autore di \\\"Knowledge Graphs as a Semantic Layer for Understanding Robotic Video\\\".\",\n    expertiseEn: [\"Software Design\", \"Data Modelling\", \"Process Optimisation\", \"Applied AI\"],\n    expertiseIt: [\"Progettazione Software\", \"Modellazione Dati\", \"Ottimizzazione Processi\", \"AI Applicata\"],\n    // NOTE: `stack` deliberately omitted — see open questions. The LinkedIn\n    // export lists no languages or frameworks; inventing chips here would be\n    // the one unverifiable claim on the page.\n    authorRole: \"Software Engineer, SERSAN\",\n    authorBio:\n      \"Software engineer. Builds internal systems end to end, from requirements and data model through to the running software.\",\n  },\n];",
      "rationale": "Structure matches the other two field-for-field and in the same declaration ORDER, so a reviewer diffing the three entries sees a clean parallel. Tone and LENGTH are calibrated: shortBio is 2 sentences (both existing ones are 2); bio is one dense paragraph like the others but shorter, which is honest — he has one role, not eight years across five institutions, and padding it to Michele's length would be the tell. Every clause is traceable to the LinkedIn export: degree + university, the L'Ultima Spiaggia scope verbatim in substance (maintenance + inventory, requirements analysis, logical/physical DB modelling, UI design, full lifecycle), the ARES internship, the publication title quoted exactly. `expertiseEn` is drawn from his three LISTED skills (process optimisation, software design, artificial intelligence) plus \"Data Modelling\", directly evidenced by the DB-modelling line. `previouslyAt` is OMITTED: that field renders under a \"Previously\" label alongside J.P. Morgan and Revolut, and a 3-month internship there reads as padding. The A.I.A./FIGC refereeing role is omitted as non-professional. His gmail address is omitted — personal address, must not be published. `roleKey`/`bioKey` are set to \"\" because the field is required by the interface but provably dead (no read site anywhere); do NOT mint new stale i18n keys for him."
    },
    {
      "file": "src/data/founders.ts",
      "lines": "113 (append after the existing getFounder export)",
      "current": "export const getFounder = (name: string) => founders.find((f) => f.name === name);",
      "change": "export const getFounder = (name: string) => founders.find((f) => f.name === name);\n\n/** Only the two co-founders. Use this on any surface whose copy says the word\n * \"founder\" — /contact's \"Talk to a founder\" and /start's \"Who reads this\".\n * `founders` itself remains the FULL team and drives the home rail + /about. */\nexport const coFounders: FounderProfile[] = founders.filter((f) => f.kind === \"founder\");",
      "rationale": "One named export makes the exclusion explicit and greppable, instead of scattering `.filter(f => f.kind === 'founder')` across two client components where a future third surface would forget it. Keeping `founders` as the FULL list (rather than renaming it to `team`) is deliberate: the home rail and /about — the two surfaces that MUST include Mattia — already import `founders` and need zero import change, so the blast radius is exactly the two surfaces that must exclude him. `authorBios`/`authorUrls` (:115-121) intentionally keep iterating the full array so Mattia gets a working /about#mattia author URL if he ever bylines an article."
    },
    {
      "file": "src/app/contact/contact-client.tsx",
      "lines": "7 and 181",
      "current": "import { founders } from \"@/data/founders\";\n…\n                    {founders.map((f) => (",
      "change": "import { coFounders } from \"@/data/founders\";\n…\n                    {coFounders.map((f) => (",
      "rationale": "EXCLUSION SITE 1. The block heading at :177-179 literally reads \"Talk to a founder\" / \"Scrivi a un founder\". Without this filter Mattia appears under it, which is factually wrong AND contradicts the page's \"Talk to the people who'll build it\" framing of two named principals. No other change needed on this page: `contactInfo` (:18-55) and `relatedLinks` (:57-69) are hand-authored and person-independent."
    },
    {
      "file": "src/app/start/page.tsx",
      "lines": "4, 102-106",
      "current": "import { founders } from \"@/data/founders\";\n…\n                {/* Founder block — the brief literally lands with one of these\n                    two people. Pulled live from src/data/founders.ts so the\n                    photos / roles / LinkedIn URLs stay in sync with /about. */}\n                <ul className=\"flex flex-col gap-4 mb-6 list-none\">\n                  {founders.map((f) => (",
      "change": "import { coFounders } from \"@/data/founders\";\n…\n                {/* Founder block — the brief literally lands with one of these\n                    two people. Pulled from `coFounders` (not the full team) so\n                    the list matches the \"read by one of the founders\" promise\n                    above; photos / roles / LinkedIn URLs stay in sync with\n                    /about. */}\n                <ul className=\"flex flex-col gap-4 mb-6 list-none\">\n                  {coFounders.map((f) => (",
      "rationale": "EXCLUSION SITE 2. The surrounding copy makes the two-person claim in prose twice — :58 \"Read by one of the founders, not a queue.\" and :90-91 \"Read by one of the founders.\" — so a third face under the \"Who reads this\" heading (:99) would contradict the page. Switching the import keeps both prose claims true with no copy edit at all. The comment is updated because it currently asserts \"these two people\" about an array that is no longer two."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "203-206",
      "current": "          <div\n            ref={foundersGridRef}\n            className=\"grid md:grid-cols-2 gap-8 max-w-5xl mx-auto\"\n          >",
      "change": "          <div\n            ref={foundersGridRef}\n            className=\"grid md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-5xl lg:max-w-6xl mx-auto\"\n          >",
      "rationale": "THE 3-UP LAYOUT, part 1. md stays a 2-column grid — byte-identical to today for the first two cards, so the md design is provably intact. lg becomes 3-up. `max-w-5xl` (64rem) across three columns with gap-8 gives ~19rem cards, too narrow for the bio paragraph at :269; `lg:max-w-6xl` (72rem) restores ~21.3rem per card, closer to the ~30rem a card gets at md today while keeping the block visually centred against the page's other max-w-5xl sections (the pillars grid at :328 stays max-w-5xl, which is correct — those are short cards)."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "207-208",
      "current": "            {founders.map((f, i) => (\n              <Reveal key={f.name} delay={i * 100} className=\"h-full\">",
      "change": "            {founders.map((f, i) => (\n              <Reveal\n                key={f.name}\n                delay={i * 100}\n                className={\n                  i === 2\n                    ? \"h-full md:col-span-2 md:justify-self-center md:w-[calc(50%-1rem)] lg:col-span-1 lg:w-auto\"\n                    : \"h-full\"\n                }\n              >",
      "rationale": "THE 3-UP LAYOUT, part 2 — the md orphan. At md the 3rd card would otherwise sit alone in the left column of row 2, which reads as a broken grid. This spans it across both columns and centres it at exactly one column's width (`50% - half the gap`; gap-8 = 2rem, so half = 1rem), so it lines up optically as a centred third card. At lg every class is reset (`lg:col-span-1 lg:w-auto`) and the card is a normal grid item. Verified `Reveal` already accepts and forwards `className` — it is used that way today at this exact line. `delay={i * 100}` needs no change; the third card just staggers at 200ms. NOTE the `id={f.anchor}` at :210 gives Mattia a working `/about#mattia` deep link automatically, matching the `authorUrls` map."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "143",
      "current": "                {isEn ? \"The founding pair\" : \"La coppia fondatrice\"}",
      "change": "                {isEn ? \"The team\" : \"Il team\"}",
      "rationale": "The eyebrow the brief calls out. \"The founding pair\" is now false on a page showing three people. `.eyebrow` uppercases via CSS, so this renders as \"THE TEAM\" without changing the string's case."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "148-164",
      "current": "              <h1 key={language} data-split-reveal className=\"font-display text-[clamp(2.5rem,8vw,5.5rem)] leading-[1.15] tracking-[-0.025em] text-ink text-balance mb-8 pb-1\">\n                {isEn ? (\n                  <>\n                    Two operators.{\" \"}\n                    <span className=\"italic\" style={{ color: \"hsl(var(--accent))\" }}>\n                      One thesis.\n                    </span>\n                  </>\n                ) : (\n                  <>\n                    Due operatori.{\" \"}\n                    <span className=\"italic\" style={{ color: \"hsl(var(--accent))\" }}>\n                      Una sola tesi.\n                    </span>\n                  </>\n                )}\n              </h1>",
      "change": "Replace the two literal words only, keeping the entire element, the `key={language}`, `data-split-reveal`, and the italic accent span untouched:\n  EN: `Two operators.` → `Three operators.`\n  IT: `Due operatori.` → `Tre operatori.`",
      "rationale": "Smallest possible edit that makes the H1 true. The counted noun is the ONLY thing that changes — \"One thesis. / Una sola tesi.\" still holds and is the line carrying the accent colour and the italic. Do not restructure the element: HeadingChoreographer owns this subtree via `data-split-reveal` and SplitText, and `key={language}` is load-bearing for the EN/IT remount (comment at :145-147)."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "196-200",
      "current": "            <p className=\"text-[11px] font-mono uppercase tracking-[0.25em] text-muted-foreground mb-4\">\n              {isEn\n                ? \"Two opposite skill sets, one thesis.\"\n                : \"Due competenze opposte, un'unica tesi.\"}\n            </p>",
      "change": "            <p className=\"text-[11px] font-mono uppercase tracking-[0.25em] text-muted-foreground mb-4\">\n              {isEn\n                ? \"Two founders, one engineer, one thesis.\"\n                : \"Due fondatori, un ingegnere, un'unica tesi.\"}\n            </p>",
      "rationale": "Directly above the now-3-up grid. \"Two opposite skill sets\" enumerates the grid's contents and becomes wrong. The replacement preserves the rhythm (comma-separated count, terminal \"one thesis\") and states the actual composition, which also quietly explains why one card's role differs from the other two."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "168-169",
      "current": "                    ? \"Deep engineering and deep commercial in the same room. Both senior. Both staffed on every engagement. No layer of juniors between you and the people doing the work.\"\n                    : \"Ingegneria profonda e dimensione commerciale profonda nella stessa stanza. Entrambi senior. Entrambi assegnati a ogni ingaggio. Nessuno strato di junior tra voi e le persone che fanno il lavoro.\"}",
      "change": "                    ? \"Deep engineering and deep commercial in the same room. Both founders senior. Both staffed on every engagement. No layer of juniors between you and the people doing the work.\"\n                    : \"Ingegneria profonda e dimensione commerciale profonda nella stessa stanza. Entrambi i fondatori senior. Entrambi assegnati a ogni ingaggio. Nessuno strato di junior tra voi e le persone che fanno il lavoro.\"}",
      "rationale": "HIGH-RISK COPY. \"Both senior\" + \"No layer of juniors between you and the people doing the work\" is a hard promise, and the page now shows a recent graduate. Scoping \"Both\" explicitly to the founders keeps the sentence syntactically identical (same 4 sentences, same cadence) while removing the contradiction. FLAG TO USER: even so, \"No layer of juniors\" sits ~40px above a third card; the user may prefer to soften that clause. It is repeated verbatim as a hard promise at who-and-why.tsx:19-23 (dead file) and as pillar 01 at about-client.tsx:93-96 (\"No layer of juniors between you and the engineer\") — that pillar is a delivery-model claim, not a headcount claim, so I would leave :93-96 alone, but the user should decide."
    },
    {
      "file": "src/app/about/about-client.tsx",
      "lines": "275-279",
      "current": "          <p className=\"text-center italic font-serif text-muted-foreground/80 max-w-2xl mx-auto mt-12 text-lg leading-relaxed\">\n            {isEn\n              ? \"The founding thesis: deep engineering and deep commercial in the same room. The combination is uncommon, and is the firm.\"\n              : \"La tesi fondante: ingegneria profonda e dimensione commerciale profonda nella stessa stanza. La combinazione è rara, ed è la firma.\"}\n          </p>",
      "change": "No change required. Verify only.",
      "rationale": "This closer says \"the founding thesis\" — an assertion about how the firm was founded, not about how many people are on the page. It survives a third card intact. Listed here so the reviewer does not 'fix' it reflexively; edits here would be churn."
    },
    {
      "file": "src/app/about/page.tsx",
      "lines": "6-7",
      "current": "  description:\n    \"Sersan was founded by two operators with opposite backgrounds: Alessandro Serratt (commercial, CAIC, dual master's) and Michele Sanna (PhD Applied Mathematics, LSE; ex-Revolut, JPMorgan, Deloitte). London-registered.\",",
      "change": "  description:\n    \"Sersan was founded by two operators with opposite backgrounds: Alessandro Serratt (commercial, CAIC, dual master's) and Michele Sanna (PhD Applied Mathematics, LSE; ex-Revolut, JPMorgan, Deloitte), joined by software engineer Mattia Scattu. London-registered.\",",
      "rationale": "The founding claim (\"founded by two operators\") stays TRUE and should not be weakened — Sersan genuinely was founded by two. Appending the third name keeps the SERP description accurate about who the page shows. Length check: the result is ~262 chars vs ~225 today; Google truncates around 155-160 anyway and this description was already over, so the delta costs nothing. `title` (:5) and `alternates.canonical` (:8) need no change."
    },
    {
      "file": "src/components/sections/who-and-why.tsx",
      "lines": "entire file",
      "current": "Orphaned component. `md:grid-cols-2` at :122; \"two founders\" EN at :87 / IT at :88; \"two people with opposite backgrounds\" EN at :294-298 / IT at :310-315; bare `founders.map` at :123.",
      "change": "DO NOT MODIFY in this change. Report to the user separately.",
      "rationale": "CONFIRMED unreferenced: grep for `who-and-why|WhoAndWhy` across src/ matches only inside the file itself. It renders to no route, so none of its two-person prose or its `md:grid-cols-2` reaches a user, and its bare `founders.map` at :123 cannot cause the unintended-inclusion bug. Editing it would inflate the diff and create the false impression that a live surface was corrected. Correct dispositions are (a) delete the file, or (b) revive it and then fix all four two-person strings — either is its own commit."
    },
    {
      "file": "src/data/translations/en.ts and src/data/translations/it.ts",
      "lines": "en.ts:87-94, it.ts:87-94",
      "current": "'founders.title' / 'founders.subtitle' / 'founders.sebastiano.role' / 'founders.sebastiano.bio' / 'founders.andrea.role' / 'founders.andrea.bio' / 'founders.mission.title' / 'founders.mission.text'",
      "change": "DO NOT MODIFY. Leave all eight keys in both files untouched, and leave `roleKey`/`bioKey` on FounderProfile.",
      "rationale": "CONFIRMED DEAD by repo-wide grep: the only occurrences of these key strings are their own definitions in en.ts/it.ts and the `roleKey`/`bioKey` values in founders.ts:47-48,82-83; the only occurrences of `roleKey`/`bioKey` are the interface declaration (founders.ts:13-14) and those four assignments. There is no `t('founders.…')` read site anywhere in src/. Since nothing reads them, a third person changes nothing about them — they are not 'made wrong' by this work, they were already wrong (keys named after sebastiano/andrea, who are not the current founders). Cleaning them is a real improvement but a strictly separate concern; bundling it here would mean touching the i18n dictionaries and the shared interface in a change whose blast radius is otherwise three components. If the user wants it, the clean version is: drop `roleKey`/`bioKey` from FounderProfile, drop the four assignments, and delete all eight keys from both dictionaries — zero runtime effect."
    }
  ],
  "hazards": [
    "CROSS-SUBSYSTEM, HIGH SEVERITY — `FOUNDER_SLUGS` is a hardcoded parallel array. src/webgl/FounderPortraitMorph.tsx:168 declares `const FOUNDER_SLUGS = [\"alessandro\", \"michele\"];` and `loadFounder(idx)` at :219-228 does `const slug = FOUNDER_SLUGS[idx] ?? FOUNDER_SLUGS[0];`. It is index-parallel with `founders[]` but NOT derived from it. If a third entry is appended to founders.ts without extending this array, `loadFounder(2)` silently falls back to FOUNDER_SLUGS[0] and loads ALESSANDRO's headshot as the third morph target — no error, no warning, just the wrong face. The robust fix is to derive the slug from the data (`founders[idx]?.anchor`), which is why the draft entry uses `anchor: \"mattia\"` — it already matches /public/founders/mattia-headshot.webp. Owner: the morph subsystem, but it is triggered by MY change to the data file. Coordinate.",
    "INITIALS COLLISION — Michele Sanna is `initials: \"MS\"` (founders.ts:77) and Mattia Scattu is also M.S. The draft uses `\"MSc\"` to disambiguate, which is defensible (BSc-holder) but slightly odd as a monogram. `authorBios` (founders.ts:115-117) is keyed by full NAME so the map itself does not collide, but any UI rendering an initials avatar would show two visually confusable badges. I did not find a consumer of `.initials` in the surfaces I read — confirm before settling on \"MSc\" vs \"MS\".",
    "`accent` is a two-value union: `\"warm\" | \"cool\"` (founders.ts:30), currently used as a 1:1 commercial/technical split and read at who-and-why.tsx:124-125 to pick `--accent-warm` vs `--accent`. A third person forces either a duplicate (`cool` shared by Michele and Mattia — the draft's choice, safe) or widening the union, which would need a new CSS custom property. Note the union is ONLY consumed by the orphaned who-and-why among the files I read, so practical risk is near zero — but the home rail may read it; verify with the morph subsystem owner.",
    "`authorBios` and `authorUrls` (founders.ts:115-121) iterate the FULL array by design, so Mattia is added to both. This is harmless and intended (a working /about#mattia author link) — but if any article in the resources data lists an author string that must map 1:1 to a co-founder, a new key appearing could change iteration-order-dependent output. I did not audit the resources data; low risk, worth one grep.",
    "The `Reveal` stagger on /about is `delay={i * 100}` (about-client.tsx:208). Three cards means the last one reveals at 200ms. Fine. But the pillar grid immediately below uses `delay={i * 90}` (:330) — after this change the founders block runs 100ms longer than before, which very slightly changes the section-to-section rhythm. Cosmetic; flagging only so it is not mistaken for a regression during visual QA.",
    "Two files carry duplicated portrait CSS with explicit 'keep them in sync' comments (about-client.tsx:445-481, founders-rail.tsx:121-169). This change requires touching NEITHER — the CSS is class-based and count-independent. But if the morph subsystem edits PORTRAIT_CSS for the third target, the About copy will silently drift. Do not extract to a shared module in this change (scope), but re-read both blocks before declaring done."
  ],
  "openQuestions": [
    "BLOCKING — `stack` / \"Ships with\" chips cannot be sourced. The LinkedIn export lists NO programming languages and NO frameworks; its only listed skills are process optimisation, software design, and artificial intelligence. Alessandro has 6 chips and Michele has 12 (founders.ts:70, :106), so an empty `stack` renders no \"Ships with\" block for Mattia at all — who-and-why.tsx:255 guards on `f.stack && f.stack.length > 0`, so the omission degrades cleanly rather than breaking. My draft therefore OMITS the field. If the user wants parity, the only defensible set inferable from the one described project (relational DB design + UI + full lifecycle) would be something like [\"SQL\", \"Postgres\", \"Python\", \"TypeScript\"] — but every one of those four is an INFERENCE, not a fact from the export. DO NOT SHIP THEM WITHOUT THE USER CONFIRMING EACH ONE. Ask Mattia directly; this is a 30-second question with a verifiable answer.",
    "Employment dates read oddly against today's date (2026-07-20): the L'Ultima Spiaggia Software Engineer role is Jun 2026 – Jul 2026, i.e. ~2 months and ALREADY ENDED, and the degree runs Sep 2022 – Apr 2026. So as written he is a recent graduate whose only titled engineering role is a two-month engagement that just finished. My draft bio states the work WITHOUT stating durations, which is honest (nothing is claimed that is not in the export) but does not volunteer the brevity. Confirm the user is comfortable with that framing, and confirm whether Mattia is now employed BY Sersan — the draft's `authorRole: \"Software Engineer, SERSAN\"` assumes yes, and the whole change presumes he belongs on the team page.",
    "The \"No layer of juniors between you and the people doing the work\" promise (about-client.tsx:168-169) and \"Senior or nothing / No layer of juniors between you and the engineer\" (pillar 01, :93-96) now share a page with a 2026 graduate. My spec scopes \"Both senior\" to the founders, which resolves the literal contradiction, but the user should make a deliberate call on whether the juniors clause stays as-is. This is the single highest-risk editorial consequence of the change and it is a positioning decision, not an engineering one.",
    "Should Mattia's role string be localised? The draft uses \"Software Engineer\" for BOTH roleEn and roleIt, matching common Italian tech usage — but the other two entries DO translate (\"CEO · Commercial Systems Lead\" → \"CEO · Lead Sistemi Commerciali\", \"CPTO · Technical Lead\" → \"CPTO · Lead Tecnico\"), so an untranslated IT value is a visible inconsistency. Alternative: roleIt: \"Software Engineer · Sviluppo\". Needs a call.",
    "His personal email (scattu.mattia@gmail.com) is deliberately EXCLUDED from the data entry — no other founder has an email field and publishing a personal gmail on a company site is a privacy and spam-surface problem. Confirm this is the intent, and confirm whether he should get a @sersan.dev / @sersan.io address before going live.",
    "`previouslyAt` is omitted. The available entries would be ARES Sardegna (IT intern, 3 months) and the A.I.A./FIGC refereeing role. On the card that field renders under a \"Previously\" heading directly parallel to Michele's J.P. Morgan / Revolut / Deloitte row, so including a short internship reads as padding by comparison. Confirm omission, or confirm ARES should be listed."
  ]
}