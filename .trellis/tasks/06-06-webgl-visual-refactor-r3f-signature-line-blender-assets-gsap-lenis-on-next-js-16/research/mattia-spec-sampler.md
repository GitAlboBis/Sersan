{
  "subsystem": "Image sampler — src/webgl/image/sampleImagePoints.ts (self-contained: imports nothing; consumed only by src/webgl/FounderPortraitMorph.tsx)",
  "summary": "The file has NO imports — it is pure DOM/canvas + typed arrays, so the two-target assumption lives entirely in its own type names and in one function.\n\nWHERE \"TWO\" IS ENCODED\n1. `PortraitPair` (lines 100-111) — literally `{ a: PortraitPoints; b: PortraitPoints; count; sharedCells; stride; gridW; gridH }`.\n2. `samplePortraitPair(imageA, imageB, spec)` (lines 398-435) — two `readGrid` calls (403-404), a two-arg union test `Math.max(readA.ink[i], readB.ink[i]) > spec.inkCut` (413), and two `emit` calls (427-428).\nNothing else is pair-shaped. `readGrid` (166-329) and `emit` (332-389) are already single-image functions, and `PortraitPairSpec` / `PortraitPoints` are already per-image. Generalising is therefore a ~15-line change to one function plus type names.\n\nHOW THE SHARED GRID WORKS\n`GRID_W=290, GRID_H=405` (FounderPortraitMorph.tsx:95-96) are passed in as `spec.gridW/gridH`. Each image is cover-cropped to that aspect, centred (lines 181-195), and rasterized to the SAME `gridW×gridH` lattice — so cell index `i = y*gridW + x` means the same normalized frame position in every portrait. The cell list is built once (412-414) and both `emit` calls walk that identical `Int32Array`, so output index `j` is cell `cells[j]` in every image. Jitter is hashed from the CELL index, not the particle index (`hash01(i*12.9898)`, lines 355-356), so all portraits jitter identically and a particle never twitches at rest. Stride subsample (419-424) is a fixed uniform `Math.ceil(sharedCells/maxCount)` taken over the union list, never random, never a duplicate pad.\n\nUNION, NOT INTERSECTION — THE KEY ANSWER\nLine 413: `if (Math.max(readA.ink[i], readB.ink[i]) > spec.inkCut) hits.push(i);`\nA cell is emitted if EITHER portrait has ink there. It is unconditional per-portrait: a cell where A has ink and B does not still exists in B's arrays, with `ink[j]=0`, `rgb[j]=` B's actual backdrop colour, `z[j]=` B's relief. So extending to three portraits is NOT an intersection problem — it is a union-growth problem.\n\nCONSEQUENCE OF MATTIA'S DARK NAVY JACKET (evidence-backed)\n`ink` is distance from the MEASURED backdrop, not darkness: `dist[i] = sqrt(0.299·dr² + 0.587·dg² + 0.114·db²)` against the median of the two TOP corner patches (lines 224-252). A dark navy jacket on a light backdrop is a LARGE `dist` → after `inkGain 1.7` and `inkGamma 0.62` it saturates near ink 1.0 over the whole torso, where Michele's white shirt sits at a much smaller `dist`. So Mattia's portrait contributes MORE cells above `inkCut` than either existing one, over a wider silhouette.\n\nTwo distinct consequences follow, and only the first is dangerous:\n(a) PER-CELL ATTRIBUTES: harmless and already handled. In A's and B's arrays those extra jacket cells get `ink=0`, and the renderer collapses them (`PORTRAIT_SIZE_MIN = 0.06`, gpgpuNodeSim.ts:1188, with the comment that ~48% of the cloud is already single-portrait-only cells at N=2).\n(b) THE CELL COUNT / STRIDE CLIFF: dangerous. FounderPortraitMorph.tsx:89-94 documents the measured number — \"290×405 → 42,087 shared cells at stride 1, which lands on the full tier's budget with headroom under the 48,000 ceiling… keep it under the ceiling so the stride stays 1 (the integer stride is a cliff — 50k cells would halve the count to 25k)\". Adding a third union term to line 413 can only INCREASE `sharedCells`, and the widest-silhouette portrait is exactly the one being added. Crossing 48,000 makes `stride` jump 1→2 and `count` drop to ~half (~24k), thinning ALL THREE faces, not just Mattia's. The disc size self-corrects (`spacingDev = sqrt(areaDev/count)`, FounderPortraitMorph.tsx:450) so it will not look sparse — it will look SOFT/low-resolution on all three. This must be measured, then the grid retargeted with the documented `scale = sqrt(wanted/measured)`.\n\n(c) EXTENT: `halfExtentX/Y` are per-portrait, computed inside `emit` (375-378, 386-387) as the 99th percentile of |px|,|py| over cells with that portrait's own `ink > extentInk (0.15)`. The caller fits the LARGER extent across portraits (`Math.max(sA.halfExtentX, sB.halfExtentX)`, FounderPortraitMorph.tsx:371-372). Mattia's saturated jacket pushes his `halfExtentY` (and possibly X) well past the other two → the max grows → `worldPerGrid` shrinks → ALL THREE faces render smaller in the stage. This is a visual regression the pair never exhibited because both existing subjects have low-ink shirts.\n\nBACKDROP REMOVAL IS PER-PORTRAIT, NOT SHARED\nThe border-seeded flood fill lives entirely inside `readGrid` (lines 254-297) and runs once per image on that image's own `dist` array, seeded from its own top row plus its own side columns above `rowLimit = gridH*0.62`. Backdrop colour is likewise measured per image (top-corner medians, 221-243). Nothing about it is pair-scoped, so a third portrait needs zero flood-fill changes. `BG_FILL_TOL = 0.055` admits a neighbour only if `dist[i] < 0.055`; the row limit stops the fill descending into the shirt/shoulders. For Mattia this is safe as long as his backdrop is LIGHT: a navy jacket has huge `dist`, so the fill cannot walk into it. The one failure mode to verify is a DARK backdrop in his headshot — then the jacket and the wall would be within `BG_FILL_TOL` of each other and the fill could eat the torso from the side columns above y=0.62·H.\n\nINK MODEL AND PLACEMENT (unchanged by this work, for completeness)\n`ink = pow(clamp((dist·inkGain − inkFloor)/(1−inkFloor), 0, 1), inkGamma) · fade`, with `fade = smoothstep` of the vertical dissolve from `fadeStart 0.62` over `fadeSpan 0.32` (lines 299-326); flood-filled backdrop cells are skipped outright (319) so the curve never has to fight the wall. `ink` becomes `sizeA/sizeB` in the compute build and drives disc size AND alpha. `inkCut 0.03` gates union membership; `extentInk 0.15` gates the extent measurement. `FACE_CY = 0.44` and `BULGE_RADIUS = 0.75` only shape the centre-bulge z term (365-371) and are currently INERT — `centerZBias` ships at 0 (FounderPortraitMorph.tsx:127). `JITTER = 0.9` is sub-cell displacement in cell units (355-356), applied to xy only.\n\nCLEANEST GENERALISATION\nMake the list the primitive and keep the pair a wrapper. `emit` and `readGrid` need no signature change at all; only `samplePortraitPair` does. Export `samplePortraitSet(images: HTMLImageElement[], spec): PortraitSet | null` returning `{ points: PortraitPoints[]; count; sharedCells; stride; gridW; gridH }`, and re-implement `samplePortraitPair` on top of it so the dev-handle `resample` path and the existing type imports keep compiling during the migration.",
  "changes": [
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "50, 78 (interface name only)",
      "current": "export interface PortraitPairSpec {\n  /** Shared offscreen grid width. One particle per kept cell — the grid area\n   * (not a `count`) is what sets the instance count. */\n  gridW: number;",
      "change": "Rename to `PortraitSpec` and add a back-compat alias immediately after the closing brace of the interface (after line 78):\n\n```ts\nexport interface PortraitSpec {\n  /** Shared offscreen grid width. One particle per kept cell — the grid area\n   * (not a `count`) is what sets the instance count. */\n  gridW: number;\n  … (body unchanged) …\n}\n\n/** @deprecated Name kept while callers migrate off the 2-portrait API. */\nexport type PortraitPairSpec = PortraitSpec;\n```\n\nThen change the three internal annotations that reference it: line 168 `spec: PortraitPairSpec` → `spec: PortraitSpec`; line 335 `spec: PortraitPairSpec` → `spec: PortraitSpec`; line 401 `spec: PortraitPairSpec` → `spec: PortraitSpec`.",
      "rationale": "The spec is already fully per-image and count-agnostic — nothing in it is pair-shaped. Only the NAME asserts a pair. The alias keeps FounderPortraitMorph.tsx:76 and :120 and :200 and :290 compiling unchanged so the sampler change can land and be verified independently of the island rewire."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "80-81 (doc comment above PortraitPoints)",
      "current": "/** Per-image, per-particle outputs. Index `j` is the SAME shared cell in both\n * images of a pair, which is what pairs the A→B morph. */",
      "change": "/** Per-image, per-particle outputs. Index `j` is the SAME shared cell in EVERY\n * image of the set, which is what pairs the A→B→C morph. A cell that is ink in\n * only one portrait still exists in all the others, there with ink 0 — the\n * cell list is a UNION (see samplePortraitSet), never an intersection. */",
      "rationale": "The contract that makes the N-way morph work is exactly this invariant, and the current wording says \"both images of a pair\", which a future reader will take as a 2-only guarantee. No code change."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "100-111",
      "current": "export interface PortraitPair {\n  a: PortraitPoints;\n  b: PortraitPoints;\n  /** Final instance count — the caller's particle count FOLLOWS this. */\n  count: number;\n  /** Shared cells found, BEFORE any stride subsample (the calibration number). */\n  sharedCells: number;\n  /** Subsample stride used to reach `count` (1 = none). */\n  stride: number;\n  gridW: number;\n  gridH: number;\n}",
      "change": "Add `PortraitSet` above it and re-express `PortraitPair` in terms of it:\n\n```ts\nexport interface PortraitSet {\n  /** One entry per input image, in input order. `points[k].xy[j*2]` and\n   * `points[m].xy[j*2]` are the SAME grid cell for every k, m — that shared\n   * index is what pairs particle `j` across every leg of the morph. */\n  points: PortraitPoints[];\n  /** Final instance count — the caller's particle count FOLLOWS this. */\n  count: number;\n  /** UNION cells found, BEFORE any stride subsample (the calibration number).\n   * GROWS with each portrait added: a cell joins if ANY portrait inks it.\n   * Watch this against `spec.maxCount` — the stride is an integer cliff. */\n  sharedCells: number;\n  /** Subsample stride used to reach `count` (1 = none). */\n  stride: number;\n  gridW: number;\n  gridH: number;\n}\n\n/** @deprecated 2-portrait view of a PortraitSet. */\nexport interface PortraitPair extends Omit<PortraitSet, \"points\"> {\n  a: PortraitPoints;\n  b: PortraitPoints;\n}\n```",
      "rationale": "An array-of-points is the only shape that scales past 3 without another rename when a fourth person joins (the compute build already has homeA..homeD). `Omit<…, \"points\">` keeps the scalar fields defined once so `count`/`sharedCells`/`stride` cannot drift between the two views."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "391-435",
      "current": "/**\n * Sample BOTH founder portraits onto one shared grid and return index-paired\n * per-particle arrays. …\n */\nexport function samplePortraitPair(\n  imageA: HTMLImageElement,\n  imageB: HTMLImageElement,\n  spec: PortraitPairSpec,\n): PortraitPair | null {\n  const readA = readGrid(imageA, spec);\n  const readB = readGrid(imageB, spec);\n  if (!readA || !readB) return null;\n\n  const total = spec.gridW * spec.gridH;\n  const hits: number[] = [];\n  for (let i = 0; i < total; i++) {\n    if (Math.max(readA.ink[i], readB.ink[i]) > spec.inkCut) hits.push(i);\n  }\n  const sharedCells = hits.length;\n  if (sharedCells === 0) return null;\n\n  const stride =\n    sharedCells > spec.maxCount ? Math.ceil(sharedCells / spec.maxCount) : 1;\n  const count = Math.ceil(sharedCells / stride);\n  const cells = new Int32Array(count);\n  for (let j = 0; j < count; j++) cells[j] = hits[j * stride];\n\n  return {\n    a: emit(readA, cells, spec),\n    b: emit(readB, cells, spec),\n    count,\n    sharedCells,\n    stride,\n    gridW: spec.gridW,\n    gridH: spec.gridH,\n  };\n}",
      "change": "Replace the whole block with the N-ary primitive plus a pair wrapper:\n\n```ts\n/**\n * Sample N founder portraits onto ONE shared grid and return index-paired\n * per-particle arrays. The instance count FOLLOWS the sampler (one particle per\n * union cell, uniformly strided down to `spec.maxCount` if the grid overshoots)\n * — never the other way round, because padding to a fixed count means\n * duplicates, and duplicates were the bug.\n *\n * THE CELL LIST IS A UNION, AND THE UNION GROWS WITH N. A cell joins if ANY\n * portrait inks it above `inkCut`; in the portraits that do NOT ink it the cell\n * is still emitted, with ink 0, and the renderer collapses it to nothing\n * (PORTRAIT_SIZE_MIN). So adding a portrait never removes coverage from the\n * others — but it does push `sharedCells` up, and `stride` is an INTEGER cliff:\n * crossing `maxCount` halves the count for EVERY portrait at once. Re-measure\n * `sharedCells` and retarget the grid (scale = sqrt(wanted / measured)) whenever\n * a portrait is added, especially a dark-clothed one, whose garment sits far\n * from the measured backdrop colour and therefore inks at ~1.0 across the torso.\n */\nexport function samplePortraitSet(\n  images: HTMLImageElement[],\n  spec: PortraitSpec,\n): PortraitSet | null {\n  if (images.length === 0) return null;\n  const reads: GridRead[] = [];\n  for (const img of images) {\n    const rd = readGrid(img, spec);\n    if (!rd) return null; // one bad decode invalidates the shared grid\n    reads.push(rd);\n  }\n\n  // --- Shared cell list: the UNION of every ink ----------------------------\n  // Index j in the output arrays is cells[j] in EVERY image, so particle j\n  // morphs from its own cell in A to the SAME cell in B, then in C.\n  const total = spec.gridW * spec.gridH;\n  const hits: number[] = [];\n  for (let i = 0; i < total; i++) {\n    let maxInk = 0;\n    for (let k = 0; k < reads.length; k++) {\n      const v = reads[k].ink[i];\n      if (v > maxInk) maxInk = v;\n    }\n    if (maxInk > spec.inkCut) hits.push(i);\n  }\n  const sharedCells = hits.length;\n  if (sharedCells === 0) return null;\n\n  // Over the tier ceiling → FIXED uniform stride. Never a random subsample\n  // (that reintroduces clumping) and never a duplicate pad.\n  const stride =\n    sharedCells > spec.maxCount ? Math.ceil(sharedCells / spec.maxCount) : 1;\n  const count = Math.ceil(sharedCells / stride);\n  const cells = new Int32Array(count);\n  for (let j = 0; j < count; j++) cells[j] = hits[j * stride];\n\n  return {\n    points: reads.map((rd) => emit(rd, cells, spec)),\n    count,\n    sharedCells,\n    stride,\n    gridW: spec.gridW,\n    gridH: spec.gridH,\n  };\n}\n\n/** @deprecated 2-portrait wrapper over `samplePortraitSet`. Byte-identical\n * output for N=2 (the union reduces to the old `Math.max(inkA, inkB)`). */\nexport function samplePortraitPair(\n  imageA: HTMLImageElement,\n  imageB: HTMLImageElement,\n  spec: PortraitSpec,\n): PortraitPair | null {\n  const set = samplePortraitSet([imageA, imageB], spec);\n  if (!set) return null;\n  const { points, ...rest } = set;\n  return { a: points[0], b: points[1], ...rest };\n}\n```",
      "rationale": "This is the entire behavioural change. `readGrid` and `emit` are already single-image and need no edit: `readGrid` measures its own backdrop from its own top corners and runs its own flood fill (lines 221-297), and `emit` walks whatever cell list it is handed (line 350). The N-ary union reduces EXACTLY to the old two-arg `Math.max` at N=2, so the pair wrapper is provably output-identical and the change can ship before the island is rewired. Returning `points[]` rather than `{a,b,c}` avoids a third rename when a fourth person is added — the compute build already exposes homeA..homeD."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "27-31 (header comment)",
      "current": "*   - SHARED GRID / SHARED CELL LIST. Both portraits are sampled onto the SAME\n *     grid and one cell list is built from the union of their ink, so output\n *     index `j` is the same cell in BOTH images. That gives A↔B index pairing\n *     for free and replaces the old radial-sector sort entirely: the morph\n *     reads as the portrait re-forming in place.",
      "change": "*   - SHARED GRID / SHARED CELL LIST. EVERY portrait in the set is sampled onto\n *     the SAME grid and one cell list is built from the UNION of their ink, so\n *     output index `j` is the same cell in ALL of them. That gives A↔B↔C index\n *     pairing for free and replaces the old radial-sector sort entirely: the\n *     morph reads as the portrait re-forming in place.\n *     COST OF EACH ADDED PORTRAIT: the union only grows, and `stride` is an\n *     integer cliff against `spec.maxCount` — see samplePortraitSet.",
      "rationale": "Line 34 also says \"the two TOP corner patches\" referring to the two corners of ONE image, which is still correct — do NOT edit that one; it is easy to misread as \"the two portraits\" and change wrongly."
    },
    {
      "file": "src/webgl/image/sampleImagePoints.ts",
      "lines": "113-114 and 121-127 (constant docs; no value changes)",
      "current": "/** Backdrop-probe patch size (px) taken from each TOP corner. */\nconst CORNER_PATCH = 14;\n…\nconst BG_FILL_TOL = 0.055;\n/** Normalized y below which the flood fill may not travel (see the fill). */\nconst BG_FILL_ROW_LIMIT = 0.62;",
      "change": "Leave all values as-is. Append one sentence to the BG_FILL_TOL doc:\n\n```ts\n * Backdrop detection is PER-PORTRAIT (backdrop colour + flood fill both run\n * inside readGrid on one image), so adding a portrait to the set cannot\n * perturb the others. It does assume each new portrait's backdrop is far in\n * colour from its subject's CLOTHING: a dark garment against a dark backdrop\n * would be within BG_FILL_TOL of the wall and the fill could walk into the\n * torso from the side seeds above BG_FILL_ROW_LIMIT.\n```",
      "rationale": "This is the only way the third portrait could break the flood fill, and it is invisible from the call site. Documenting it at the constant is where the next person will look. No behaviour change — Mattia's navy jacket is safe against a LIGHT backdrop precisely because the fill is chromatically gated at 0.055 and his jacket's dist is large."
    }
  ],
  "hazards": [
    "STRIDE CLIFF — the highest-risk consequence, and it is silent. FounderPortraitMorph.tsx:89-94 records the measured calibration: 290×405 → 42,087 union cells at N=2, against a full-tier ceiling of 48,000 (MAX_COUNT_BY_TIER, line 83-86). Adding a third portrait to the union at line 413 can only increase that number, and the added subject is the one with the widest high-ink area (dark jacket → large backdrop distance → ink ≈ 1.0 across the whole torso). If the 3-way union lands above 48,000, `stride` jumps 1→2 and `count` halves to ~24k for ALL THREE portraits. It will not look sparse (disc size auto-compensates via `spacingDev = sqrt(areaDev/count)`, FounderPortraitMorph.tsx:450) — it will look uniformly SOFT, which is much easier to ship by accident. MUST be measured before tuning anything else: read `sharedCells` off `window.__sersanFounderMorph.getSampler()` (surfaced at FounderPortraitMorph.tsx:780) and, if over ~46,000, scale the grid by `sqrt(46000 / measured)` per the retarget recipe already in the comment.",
    "EXTENT INFLATION — `halfExtentX/Y` are per-portrait (emit, lines 375-387: 99th percentile of |px|,|py| over cells with that portrait's own ink > extentInk 0.15) and the caller fits the MAX across portraits (FounderPortraitMorph.tsx:371-372, `Math.max(sA.halfExtentX, sB.halfExtentX, 1e-3)`). Mattia's jacket inks near 1.0 well past extentInk across the torso, so his halfExtentY will exceed both existing ones and shrink `worldPerGrid` → all three faces render SMALLER in the stage than they do today. This is a regression on Alessandro and Michele caused purely by adding Mattia. Mitigations, in order of preference: (a) raise `extentInk` above the jacket's ink so only facial detail counts — but verify it does not also drop Michele's cheeks; (b) lower `fadeStart` (currently 0.62) so the dissolve eats the jacket earlier; (c) crop the Mattia asset tighter. Do NOT special-case a per-portrait spec — one shared spec is what guarantees the shared grid.",
    "THE COMPUTE BUILD CANNOT CARRY A THIRD COLOUR OR INK TODAY. `PortraitMorphOpts` (gpgpuNodeSim.ts:841-870) declares only `colorsA/colorsB` and `sizeA/sizeB`, and the mixing expressions are strictly two-way: `portraitInkExpr = mix(sizeABuffer, sizeBBuffer, portraitMorphExpr)` (1175-1181) with `portraitMorphExpr` built from `morphN` (uMorph) alone (1167-1173). The four home targets (homeA..homeD, 876-879) are NOT matched by four colour/ink targets. So this sampler change alone gets you Mattia's POSITIONS but Michele's FACE on the C leg. Extending PortraitMorphOpts to colorsC/sizeC and chaining the mix on uMorph2 is a separate, mandatory change owned by the gpgpu subsystem — flag it to the orchestrator as a hard dependency, not a follow-up polish item.",
    "DO NOT reintroduce any per-pixel chromatic backdrop test to \"handle\" the dark jacket. Lines 254-271 and 5-12 are an explicit, twice-burned prohibition: a lumCeil/neutralSat or inkGate test cannot distinguish a lit scalp or white shirt from a white wall and punches holes through the subject. MEMORY.md records the same rule (\"separate subject from backdrop SPATIALLY, never by colour threshold\"). The flood fill is the only sanctioned mechanism.",
    "`readGrid` returning null for ANY image invalidates the whole set (the proposed early return). At N=2 a failed decode already nulled the pair; at N=3 a missing Mattia asset would now blank the ENTIRE founders section rather than degrading to two. FounderPortraitMorph.tsx:219-229 `loadFounder` already falls back from `-headshot.<ext>` to `founders[idx].image`, so a total failure requires both assets missing — but FOUNDER_SLUGS (line 168) is `[\"alessandro\", \"michele\"]` and `loadFounder` falls back to `FOUNDER_SLUGS[0]` for an out-of-range index, meaning an un-updated slug list would silently sample ALESSANDRO TWICE as target C rather than erroring. That failure is invisible in the console.",
    "mattia-headshot.webp is 74 KB against michele 113 KB and alessandro 177 KB. A dark, flat jacket compresses well so this is expected, but it also means lower high-frequency detail in the FACE region. If the face reads mushy after the morph lands, suspect source detail before suspecting the ink curve.",
    "Both `emit` calls (now `reads.map`) allocate count×(2+3+1+1) floats per portrait. At 42k cells the third portrait adds ~1.2 MB of Float32Array plus a third full GridRead (5 × gridW·gridH floats ≈ 2.3 MB) held during sampling. Not a problem on the full tier, worth a glance on lite (maxCount 16,000)."
  ],
  "openQuestions": [
    "What is the ACTUAL 3-way union cell count at 290×405? This is the single number that decides whether GRID_W/GRID_H must change, and it cannot be derived by reading code — it depends on Mattia's silhouette. Measure it first, before writing the island rewire: land the sampler change, then read `sharedCells` and `stride` from `window.__sersanFounderMorph.getSampler()` (FounderPortraitMorph.tsx:772-786). If `stride > 1`, retarget the grid with `scale = sqrt(46000 / sharedCells)` and re-measure.",
    "Is mattia-headshot.webp's backdrop LIGHT (like the other two studio headshots) or dark? If dark, the navy jacket may fall within BG_FILL_TOL (0.055) of the measured backdrop colour and the border-seeded fill could walk into the torso from the side seeds above BG_FILL_ROW_LIMIT (0.62·H), deleting the bust. Verify by decoding the image and comparing the top-corner median against a torso sample — or empirically, by checking whether his shoulders survive the first render. I did not decode the webp.",
    "Does the caller want the C leg to share ONE world scale with A and B (today's behaviour, FounderPortraitMorph.tsx:371-377 fits the max extent) or to rescale per leg? Sharing one scale is what keeps the morph travel short and the registration pixel-pinned; per-leg scaling would make each face fill the stage but introduce a zoom during the morph. This is a design call the island owner must make, and it determines whether the extent-inflation hazard above needs fixing at all.",
    "Should `samplePortraitPair` be deleted outright once the island is rewired, or kept? It is referenced at FounderPortraitMorph.tsx:313 and :560 (the dev `resample` path). Keeping it costs 8 lines and de-risks landing the sampler independently; deleting it after the rewire is a trivial follow-up. I recommend keeping it through the rewire, then removing it in the same commit that removes the last call site."
  ]
}