# critic-contracts — adversarial review of SYNTHESIS.md against HANDOFF_FOUNDER_MORPH.md contracts 1–16 and the working tree

Date 2026-08-27 (evening). Lens: HANDOFF contracts 1–12 (+ the 13–16 already in the tree), 4-way sequencing (`uMorph` must reach 1.0 before `uMorph2` leaves 0), shared-grid union invariant, stride cliff, asset-side vs threshold fixes, dark-clothes rule, hero regression risk through the shared `gpgpuNodeSim.ts`. Everything below was read from `C:/Users/alber/Desktop/Sersan` working tree (HEAD `f38e08f` + uncommitted) and the Lusion bundle copy in `dossiers/lusion-bundle/hoisted.pretty.js`. Line numbers are the tree's.

## Confirmed

1. **Sequencing invariant holds at N=4, structurally.** `FounderPortraitMorph.tsx:401-404` `applyMorph(b,p)`: `uMorph = clamp(p,0,1)`, `uMorph2 = clamp(p−1,0,1)`, `uMorph3 = clamp(p−2,0,1)` — all three from ONE scalar, so `uMorph2 > 0 ⇒ uMorph === 1` by construction. The clock clamps toward the TARGET (`:1119-1120` `target > cur ? Math.min(cur+step,target) : Math.max(cur−step,target)`) — contract 9 respected (no limit cycle at the interior stages B and C). Dev override and the touch scrub (`:1097-1103`) both feed the same `applyMorph`.
2. **Kernel chain is the chained `mix`, with the same stagger everywhere.** `gpgpuNodeSim.ts:1497-1509`: `m = clamp((uMorph − r·0.55)/0.45)`, `target = mix(hA,hB,ss(m))`, then `mix(target,hC,ss(m2))`, `mix(target,hD,ss(m3))`. Worst-case particle (r→1) reaches `m = (1−0.55)/0.45 = 1.0` exactly at `uMorph = 1`, so `target == hB` before leg 2 opens (contract 10). Colour (`:2167-2184`), ink (`portraitInkExpr`, `:1664-1690`) and normal (`:1867-1880`) chains use the identical `hash(instanceIndex)·0.55 / 0.45` windows → shading, presence and position cross targets in lockstep.
3. **Relief scaling is portrait-gated and does not touch the chain order.** `:1517` `if (hasPortrait) target.z.mulAssign(reliefN)` sits AFTER the three mixes; `uRelief` is `uniform(1)` created unconditionally (`:1344`) but only referenced behind `hasPortrait`, so it never enters the hero graph. Island writes `uRelief = REST_RELIEF + (1−REST_RELIEF)·env` (`FounderPortraitMorph.tsx:1234`, `REST_RELIEF 0.2` at `:240`, `env = sin(legFract·π)` at `:1136`) → 0.2 at every locked stage, 1 mid-leg. SYNTHESIS §1.7 "≈7 % of face height at rest" = 0.2 × 0.34 — arithmetic checks out (and `Z_RELIEF_DEPTH_FRAC 0.34` is effectively non-binding: the depth field spans ±45 grid px = `spec.depth 90` (`sampleImagePoints.ts:480-481`) against a presence-extent face height ≈ 266 grid px → `zNorm = min(1, 0.34·266/45) = 1`, so the relief is the sampler's own 90 grid px ≈ 0.67 × head width, i.e. Lusion's 0.58 order).
4. **Hero regression risk through the shared file is low (by code inspection, not by shader dump).** `HeroTextParticles.tsx:443-452` passes `homeAWorld` ×4 and NO `portrait` → `hasPortrait = !!portrait = false` (`:1323`), so `hasPortraitSize/C/D/Lit` are all false; every new node (`packTint` lit layout, `unpackTintColor`, `litFrontExpr`, `litBlurExpr`, `litLightExpr`, `litModelPos = positionBuffer.toAttribute()`, `uLightPos`, tone path) is created only inside `hasPortraitLit ? … : null` (`:1804, 1885, 1893, 1920, 2170-2196, 2226-2231, 2304`). `git diff HEAD -- gpgpuNodeSim.ts | grep '^-'` removes only comments and portrait-only lines (old `packTint`, old 3-way tint chain, old `smoothstep(0.5,0.34,rr)` ternary). Hero literals preserved: edge `smoothstep(0.5,0.12,rr)`, `Discard(alpha < 0.004)` (`:2305`), `AdditiveBlending` (`:2320`). `HeroTextParticles.tsx` itself is untouched (not in `git status`). Caveat in "Open questions" — the HANDOFF's own lesson (contract 6/7) is that inspection is not evidence.
5. **Root cause 1 (chromatic `ink`) and its fix are as described.** Legacy branch `sampleImagePoints.ts:432-437` `ink = ((dist·inkGain − inkFloor)/(1−inkFloor))^inkGamma · fade`; depth branch `:422-431` `ink = smoothstep(depthCut ± depthEdge)(dep) · fade`, both after `if (bgMask[i]===1) continue` (`:421`) — presence AND flood fill, as the HANDOFF §"Depth matte" says. Alpha knee `smoothstep(0,0.1,ink)` `:2274`, `cov²` `:2282-2288`, `Discard(alpha < 0.02)` `:2305` — SYNTHESIS §1.2 numbers verified.
6. **Zero new bindings claim is consistent with the code.** Lit layout `[rgb24, nx, ny, ink]` packed in `packTint` (`:1361-1386`, `enc(r)·65536 + enc(g)·256 + enc(b)` ≤ 2^24−1, exact in f32), decoded `:1850-1858`; normals rebuilt with `sqrt(max(1−x²−y², 0))` (`:1862-1865`). `positionBuffer.toAttribute()` for `litModelPos` reuses the same attribute already used by `portraitDistExpr` (`:1778-1783`) → no extra vertex buffer. Uniforms (`uRelief`, `uLightPos`, look knobs) are uniform-buffer entries, not storage bindings. Compute stays 8/8 (position, velocity, homeA–D, start, delay — `:1486-1491, 1502, 1507`).
7. **Union/stride machinery unchanged and monotone.** `samplePortraitSet` `:590-631`: a cell enters if `maxInk > spec.inkCut` over ALL portraits (`:614`), `stride = ceil(sharedCells/maxCount)` integer, `cells[j] = hits[j·stride]` (`:621-625`) — contract 11 intact. Ceiling raised to `full: 80000` (`FounderPortraitMorph.tsx:184`), `lite: 20000`, `TOUCH_GRID_SCALE 0.52` (`:200`).
8. **Depth twins were made with the `base` model.** `scripts/generate-founder-depth.mjs:39-44` default `"base"` → `onnx-community/depth-anything-v2-base`; the four `public/founders/*-depth.webp` (47–52 KB, 16:11–16:13) are untracked (`??`). The script reads `<anchor>-headshot.*` (`:61`) — i.e. for Mattia the WASHED file (contract 12), see Amendment 6. The script's min-max normalisation is over the whole map including the wall (`:70-80`).
9. **Placeholder Alberto**: `alberto-headshot.webp` and `alberto-tuveri.webp` are both 15,674 B; `alberto-depth.webp` (52,160 B) is therefore a depth map of a monogram card. Correct as flagged.
10. **Bloom threshold on the home route is 1.0**, not 0.8: `fxStore.ts:279-281` (`1.1 / 1.0 / 0.7`), `routeFxStore.ts:64` default 1.0; only `/trust` overrides to 0.92 (`:87`). SYNTHESIS §3.10 is right; HANDOFF contract 14 ("bloom (soglia 0.8)") quotes Lusion's value as if it were ours — doc bug, see Amendment 9.
11. **Lusion's model-space quad and additive/no-depth material** — verified verbatim in `hoisted.pretty.js`: `basePointSize=0.009*(1.+pow(v_blurriness,1.5)*8.)*frontFaceMultiplier; pointSize=max(basePointSize,12./u_resolution.y); subpixelMultiplier=pow(basePointSize/pointSize,1.5); pos.xy+=position.xy*pointSize*step(0.003,light)*linearStep(0.0,0.75,u_activeRatio); … gl_Position=projectionMatrix*modelViewMatrix*vec4(pos,1.0)`; container `scale.set(27.5, 27.5, 16)`.

## Refuted / wrong

1. **"HANDOFF diff adds only the N=4 addendum — nothing about the depth matte" (SYNTHESIS §1.7 "Docs / QA", §4B file map, §5.1 step 7).** False. `git diff HEAD -- HANDOFF_FOUNDER_MORPH.md` (+72 lines) contains a full section `## Depth matte + ritratto illuminato + navigazione a frecce (2026-08-27, pomeriggio)` (`HANDOFF_FOUNDER_MORPH.md:297-329`) with the diagnosis, the 4-step solution, **new contracts 13–16** (comb vs `REST_RELIEF`; pointer light must not cross the view axis; tint layout is per BUILD; `photo` compresses luminance), the new handles (`setLook/getLook/setBlend/setDepth`) and the open items. Step 7 of the plan ("update the HANDOFF, new section") is already done; what is missing is only (a) restating contracts 1/2/12 in the light of presence-ink and (b) the depth-model licence rule.
2. **The "one-viewport gate" invariant and the "simulateGesture('down') ×4 → then release" verification are stale.** The scroll-jack gate was REMOVED by owner request the same day: `founders-rail.tsx:129-134` ("the scroll-jack gate … is GONE"), `:1125-1160` (`step()` no-ops past either end, `getGate().engaged` is hard-coded `false`, "Nothing here touches Lenis"), buttons `[data-founders-prev]/[data-founders-next]` (`:2015, 2023`) and ← → keys (`:1180-1192`). HANDOFF `:316-318` records it. SYNTHESIS §4 "Common invariants … the one-viewport gate", §4B Verification "×4 → A→B→C→D then release", and the store header comment in `foundersMorphStore.ts:10-32` (still describing the gate) are all out of date. The HANDOFF's own assertion 2 ("il gate NON rilascia sui nodi interni", `:203-208`) is now void.
3. **"Union not re-measured" (§1.5, §1.7 "Must be re-measured").** HANDOFF `:306` records an in-browser measurement after the depth matte: **46.5k cells, stride 1** against the 80k ceiling (with the placeholder Alberto). It must be re-measured when the real Alberto lands, but the "unmeasured cliff" framing is wrong: the margin is ~33k cells.
4. **Root cause 3 as stated — "Lusion draws ~6k points at ≥12 px, discs ≈2× pitch; SerSan must go to ≈2× pitch and ≈15–25k particles" — does not follow from the bundle.** `pointSize` is added to `pos.xy` in MODEL space *before* `modelViewMatrix` (verbatim above), so `0.009` and `12./u_resolution.y` are model units, not pixels. In those units the nearest-neighbour spacing is 0.0118 (SYNTHESIS §2.2), ×1.3 (`pos *= 1.3`) = 0.0153; the in-focus quad side is `0.009` → **0.59 × pitch**, and the floor `12/u_resolution.y` = 0.0089–0.0133 for 900–1350 px → **0.6–0.9 × pitch**. Lusion's in-focus discs are therefore *smaller than the lattice pitch*, close to the tree's `DISC_PITCH_LIT 1.15` — not 2×. What makes the head read solid is the DoF law: with `v_blurriness = min(1, |depth − 0.5|·2.5)` and z p50 = 0.79 (depth p50 = 0.21) the MEDIAN point has b ≈ 0.72 → size × (1 + 8·0.72^1.5) ≈ 5.9 → ≈3.5 × pitch, alpha × (1−b)³·0.8 ≈ 0.02, shade × (1 − 0.5b): a thin crisp shell at the focal plane over a large, dim, additive bokeh cloud, plus bloom amount 12.5 at threshold 0.8. Density: ~170 samples across Lusion's [−1,1] extent vs SerSan's ≈135 grid cells across the 559 px head (559/1200·290) — SerSan is already *not denser* than Lusion across the head. **B2's grid halving to 150×210 (≈70 cells across the head) would make SerSan sparser than the reference and is not supported; the pitch multiplier is not the lever, the bokeh law + bloom are.** (Also: "≥12 px" as literal pixels would need `u_resolution` to be in model units, which no dossier established.)
5. **B4 "DPR cap 1.5 for the lit build (`dprNow` → 1.5) halves fill cost" is ineffective as written.** `dprNow` (`FounderPortraitMorph.tsx:835-838`) only sizes `spacingDev/discDev/defPointSize`; the render still multiplies by `uPixelRatio = min(gl.getPixelRatio(), 2)` (`:1247-1248`, `gpgpuNodeSim.ts:2084-2085`) and the canvas is the site-wide renderer (`antialias:false`, Scene.tsx). Lowering `dprNow` alone would only make the discs *larger relative to the pixel grid* (defPointSize ∝ 1/dprNow while the real pixel ratio stays 2) — it changes the look, not the fill cost. A real 1.5 cap is a renderer-level (site-wide) decision, outside this island.
6. **B3.1 "perspective pre-divide in the kernel" is under-specified and, as written, leaves a comb.** The group is placed OFF-AXIS: `group.position = ((cx − vw/2)·k, (ih/2 − cy)·k, −(CAMERA_Z − dolly))` in camera space (`FounderPortraitMorph.tsx:1164-1169`) — the stage centre is not the viewport centre. Projected x = (Gx + lx)·f/(d − z). Pre-dividing only the LOCAL `lx` leaves `Gx·f·z/d²`; with Gx ≈ 3–5 world units, |z| ≤ 0.85 (0.34 × ~5-unit face) and d = 12 that is 0.02–0.03 world ≈ 1–1.6 lattice pitches (`worldPerGrid` ≈ 5/266 ≈ 0.019) — still a comb at the ear ramps. It also breaks under the group rotation it coexists with (sway 0.02 + parallax 0.16 rad, `:1192-1200`), which SYNTHESIS itself concedes. See Amendment 3.
7. **B5 "bake AO into the packed rgb — zero shader change" is mostly neutralised by the tone path.** `gpgpuNodeSim.ts:2185-2196`: `lum = dot(lin, Y)`, `lumC = mix(1, lum, uPhoto)` with `photo 0.35`, `chroma = lin / max(lum, 0.02)`, `tone = mix(chroma·lumC, monoTint·lumC, mono 0.8)`. AO multiplied into `lin` cancels out of `chroma` entirely and survives only through `lumC` at 35 % strength. The "lit plate vs scan" separation SYNTHESIS wants from AO would arrive at roughly a third of its intended contrast. See Amendment 4.
8. **HANDOFF contract 14's "bloom (soglia 0.8)"** is wrong for this site (home threshold 1.0, `fxStore.ts:280`) — it describes Lusion. Not a SYNTHESIS error, but the plan inherits the sentence in §1.7 without correcting it.
9. **Contract 10's warning "don't copy the hero: it opens the second leg at ≥ 0.95"** is now moot: `HeroTextParticles.tsx:41-42` "B/C/D are fed homeA and uMorph/uMorph2/uMorph3 stay 0 forever — the legs never play". Harmless, but SYNTHESIS §1.6 / §4 should not cite it as a live constraint.

## Amendments

1. **Replace B2 with "adopt Lusion's DoF law, keep the grid".** Keep `GRID 290×405` (density already ≤ Lusion across the head) and `DISC_PITCH_LIT` near 1.0–1.3 (Lusion's in-focus 0.6–0.9 × pitch, ours slightly larger because we have no bloom ×12.5). Move the size law to Lusion's shape: `size × (1 + k·b^1.5)` with `k` → 6–8 (`DEFAULT_LOOK.bokeh` 1.2 → 6–8), alpha × `(1−b)³·c` instead of the current `(1 − 0.55b)` (`:2304`), and a fragment edge `range` that widens with b (already `0.34 − 0.3b`, fine). Choose `focusRange` so the median bust point has b ≈ 0.5–0.7 (Lusion: 0.72), i.e. the focal shell is thin — this is what produces "a crisp face over a glowing volume". The pixel floor + `subpixelMultiplier` from B2 stays useful (`max(px, uMinPx)`, `mul(pow(base/px, 1.5))`), but set `uMinPx` ≈ 2–3 devpx, not 6·dpr, or the additive sum blows out (contract 14). Retune `DEFAULT_EMISSIVE_LIT` after.
2. **DoF must see the FULL relief while the projected relief stays parked (zero bindings).** Today `litBlurExpr` uses `portraitDistExpr` from the simulated position (`:1778-1783, 1893-1901`), whose z is already × `uRelief 0.2`, so b ≈ 0 at rest and Amendment 1 would do nothing at the locked stages. Reconstruct the unscaled depth in the vertex stage: `zFull = pos.z / max(uRelief, 0.05)` (pos = `positionBuffer.toAttribute()`), `distFull = dist − (zFull − pos.z)` (group is camera-aligned, so local z ≈ view depth), and feed `distFull` to `litBlurExpr`. This gives Lusion's bokeh cloud at rest with NO lateral displacement — i.e. no comb — which is exactly what B3 was trying to buy. Mid-leg (`uRelief → 1`) it degenerates to the current expression. (The spring flight is on the *scaled* target, so nothing moves.)
3. **Downgrade B3.1; reorder B3 after Amendments 1–2 and re-measure the comb first.** If B3.1 is still wanted, it must pre-divide the *camera-space* offset: `x' = (lx + Gx)·(d − z)/d − Gx` with a new `uGroupOffset` vec2 uniform written from the same `(cx − vw/2)·k, (ih/2 − cy)·k` the island computes at `:1166`, and it must be blended out not only by `env` but also by `hover` (the rest parallax rotates the group). B3.2 (grey dilation radius 2–3 cells before the blur, `reliefField` `:455-483`) is the safer half and is asset-side/sampler-side, consistent with contract 12's "fix in the asset, not with a threshold". Contract 13's rule stands: do not raise `REST_RELIEF` before looking at the right ear enlarged.
4. **B5 AO must bypass the `photo` compression.** Pack `w = floor(presence·255)·256 + floor(ao·255)` (exact in f32) in `packTint`, decode both in the vertex stage BEFORE `portraitInkExpr` is consumed (ink feeds `sizeNode` `:2087`, the alpha knee `:2274` and `vInkF`), and multiply the LIGHT term, not the colour: `min(ambient + diffuse·diff + rim, 1) · mix(1, ao, uAoK) + scan` in `litLightExpr` (`:1920-1950`). Zero bindings; one extra decode. Baking into rgb (SYNTHESIS's "zero shader change" variant) should be dropped.
5. **B4: strike it, or make it honest.** Either leave DPR alone, or (site-level, separate decision) cap the renderer's pixel ratio in Scene.tsx; do not touch `dprNow` in isolation. If the additive discs are the fill concern, the lever is `uMinPx`/bokeh, not DPR.
6. **Depth twins: generate from the CLEAN photo, not the washed `-headshot`.** Contract 12's wash is a COLOUR correction for the sampler's ink; with presence ink it no longer serves ink at all (presence ≡ 1 inside the bust, the dissolve is `fadeStart 0.55/fadeSpan 0.3` for everyone) and only still matters for the packed colour, where `photo 0.35 × (1−mono 0.8)` already flattens it. Feeding a washed bust to a monocular depth model risks a hallucinated depth ramp at the collar. `founders[].image` (`mattia-scattu.webp`, `michele-sanna.webp`, `alessandro-serratt.webp`) are the same 1200×1800 framing (checked with sharp) — change `generate-founder-depth.mjs:61` to prefer `<anchor>-<slug>` clean sources (or add a `--source` flag), then re-measure `depthCut` per Amendment 7. Re-state contract 12 in the HANDOFF as: "wash only if the packed colour still dominates after `photo`/`mono`; the depth twin always comes from the unwashed photo".
7. **B1 stays, with two additions.** (a) Regenerate with `--model small` (Apache-2.0) — cheap and removes the CC-BY-NC question — and make `small` the script default (`:39-44`). (b) Write the twins as 16-bit PNG (or keep 8-bit but blur ≥ 2 cells): at 8 bits the sampler's `(sm − 0.3)/0.7 → ±45 grid px` maps one quantisation level to 0.5 grid px, so an un-blurred step between neighbours yields a normal tilt of ≈14°; the current 3×3 box blur (`:462-478`) only divides that by ~3 — expect faint terracing on smooth skin under the rim term. Head-only normalisation (`dNorm^0.7`) is fine but changes the bimodal gap, so `depthCut/depthEdge` must be re-derived from the histogram each time (the tree comment's "wall ≤ 0.19 / bust ≥ 0.35" is a `base`-model, full-map number).
8. **Verification protocol must be rewritten for the arrow navigation.** Replace "simulateGesture ×4 then release" with: buttons/keys advance one leg each; `getGate().engaged === false` always; page never held (`__lenis` keeps scrolling through the section); parked on B on consecutive frames `progress 1, uMorph 1, uMorph2 0, uMorph3 0` exact; parked on C `uMorph2 1, uMorph3 0`; on D `uMorph3 1`; `step()` at A with dir −1 and at D with dir +1 are no-ops and the buttons read `aria-disabled`. Update `foundersMorphStore.ts:10-32` header (still describes the gate) and HANDOFF `:188-208` (assertion 2).
9. **Docs**: fix HANDOFF contract 14 (threshold is 1.0 on home, 0.92 on `/trust`; Lusion's 0.8 is the reference, not ours); add the licence rule; restate contracts 1 ("tone comes from size" → on the lit path tone comes from normal·light, size is presence × bokeh), 2 (spatial separation is now depth-cut AND flood fill), 12 (see Amendment 6); mark contract 10's hero note as historical.
10. **Hero regression gate — make it evidence, not inspection.** Before committing the engine half, dump the hero build's generated WGSL (three r184: `renderer.debug.getShaderAsync(scene, camera, mesh)` or `material.needsUpdate` + `builder.vertexShader/fragmentShader`) at HEAD and in the tree and diff them; the HANDOFF's contract 6/7 history is exactly this class of "byte-identical by reading" claim. Same for the N=3 portrait build if the content half stays out.
11. **Commit split (agree with §5.1 step 1) plus one more cut**: the tree also carries the gate removal in `founders-rail.tsx` (−503/+119 lines) and `lenis-singleton.ts/scroll-snap.ts/spine.ts` edits — that is a third, behavioural half (navigation), separate from engine and content, and it changes the store's documented model. Commit it on its own with the store header rewritten.

## Open questions

1. **Does `u_resolution` in Lusion's faces shader carry pixels?** If it does, the "12 px floor" is really 12/900–12/1350 model units ≈ 0.6–0.9 × pitch (Refuted 4); if it is a normalised value the floor could be larger. Nobody in the 14 dossiers resolved the head's on-screen pixel size, so Lusion's *pixel* pitch is unknown; only the pitch-relative ratios above are solid.
2. **Owner's live-tuned 1.15× pitch ("nuvola, non foto fusa", HANDOFF `:306`) vs SYNTHESIS's 2.0–2.4×.** The owner made that choice with the render in front of him; the plan overrides it from a misread of Lusion's units. Needs an explicit owner call after Amendment 1 is visible.
3. **Section-scoped bloom.** Lusion raises bloom amount 4 → 12.5 only in the team view. `PostFXNodes.tsx:175-181` says strength/radius are compositing knobs and the threshold is the selective-bloom contract; is a founders-in-view intensity boost (like `/trust`'s route override) acceptable, or does the site's "bloom only above 1.0" contract forbid it? This is a bigger lever than any disc-size change.
4. **CC-BY-NC-4.0 scope.** Whether depth maps *produced* by the base model are "adapted material" under CC-BY-NC is unsettled; regenerating with `small` (Apache-2.0) makes the question moot at ~10 s cost, so this is only relevant if `base` quality turns out to matter.
5. **Lite/touch build with the lit path**: `tier lite` uses the same `lit` build (additive, per-vertex light, bokeh growth) at `TOUCH_GRID_SCALE 0.52` (≈12–13k cells at 40 % presence) — never measured on a phone; the bokeh growth of Amendment 1 multiplies fill on exactly the devices with the least fill budget. Needs a phone measurement or a lite-specific `bokeh` cap.
6. **Normal chain gates differ from the colour chain** (`hasPortraitSizeC/D` vs `hasPortraitC/D`, `:1873-1876` vs `:2180-2183`). Today both are true together; if a caller ever passed `colorsC` without `sizeC`, colour would cross to C while normals stayed on B. Harmless now, worth unifying.
7. **Real Alberto**: the union will grow by his presence area (~10–12k cells on the 290×405 grid) → ~58k vs 80k ceiling: safe, but the number must be written into `MAX_COUNT_BY_TIER` (contract 11) after a hard reload.
